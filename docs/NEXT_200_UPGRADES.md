# Next 200 upgrades after v8.0.0

This backlog is ordered for implementation, not brainstorming. Earlier slices reduce risk or create leverage for later slices. It is an execution queue, not a release claim: every slice remains unadvertised until it satisfies the acceptance path below.

## Campaign status

- **Current full-regression closure (2026-08-29):** the complete current source suite collected **2,303 tests** and finished with **2,281 passed, 22 skipped, 0 failures, 0 errors** in 2,206.702 seconds. The first run is retained separately: nine PDF-preview failures shared a source-worker import-boundary problem, and one unattended-build test expected the former direct `-Offline` syntax. The source worker now uses the explicitly selected development interpreter's installed packages while frozen Store workers retain `PYTHONNOUSERSITE=1`; the canonical child build's equivalent conditional Offline argument is documented. The exact 50-test PDF/sandbox/build group passed before the full rerun. Evidence: `dist/ga_today/evidence/08_full_pytest_current_r2.xml` (diagnostic failure) and `08_full_pytest_current_r3.xml` (clean replacement). Skips remain explicit: unavailable archived v7/GA evidence, optional native-audio input, and unavailable Windows symlink/POSIX-mode capabilities. This is not universal 200-slice E2E or release certification.

- **Current frozen aggregate verification (2026-08-29):** the exact essential-tier r2 package `ad783aac618370a9f071a8b49da957ddde56373c831f00676da4ceb453088b9c` passed current tracked-DOCX, structured outline, sentence support, exact-section citation/quote drafting, requirement profile, revision rationale, dual view, argument matrix, export provenance, procedure pathway, deadline dependency, procedure review, family/finance, runtime management, navigation continuity, static accessibility assets, privacy/security, reliability, and encrypted backup/transfer runners. Each report is retained as `08_v8_*_r2_current.json` (the successful citation/quote rerun is `08_v8_source_bound_drafting_e2e_r2_section_current.json`; the earlier wrong-fixture attempt remains diagnostic). These are bounded fictional frozen API/asset journeys; only separately recorded browser interactions count as browser evidence. The 155–200 focused acceptance group also passed **104 tests across 41 files**. External trust, human review, pilot, installed Store, and WACK requirements remain blocked, not simulated into completion.

- **Queue priority correction (2026-08-27):** the recurring task now follows the user's newer FAST INTERCHANGE planning/prerequisite request, not unrelated feature expansion. The Slice 30 generic source-library provenance defect was repaired on 2026-08-28: public inventory, detail, span, lineage, Law Court enrichment, and rule-history reads now use only hash-verified artifacts materialized in the selected active authority build and fail closed without one. The remaining FAST INTERCHANGE preview/source-binding work and all real-model admission inputs remain separately blocked.

- **Productivity no-active-matter repair (2026-08-29):** all Productivity Studio handlers now defer active-matter resolution until the canonical error boundary, so a normal status, mutation, or source-inspection click without a selected matter returns the actionable, non-mutating `409 active_case_unavailable` response instead of an unhandled server error. The focused canonical suite passed **8 tests, 0 failures, 0 errors, 0 skipped**; Python compilation, production JavaScript syntax checks, and diff checks passed. This is source-checkout/API evidence only, not a frozen-runtime or MSIX qualification. Evidence: `dist/ga_today/evidence/08_productivity_no_active_matter_regression.xml`.

- **Slice 30 immutable-source and browser hardening (2026-08-27, repaired 2026-08-28):** coverage now reads hash-verified parsed artifacts from the selected immutable generation, not mutable ingestion files. Its source browser and exact-span API bind to that generation and fail closed on a changed build or corrupted artifact. A fictional production-UI action exposed and repaired a body-level view click handler that reset the drawer, plus a delayed-hover race that replaced an opened exact-source dialog. Browser review, blocker display, immutable-source browsing, exact-source inspection, and visible review-required provenance passed against the real source desktop API; DOM and screenshot evidence were retained. The formerly open generic initial source-library fallback is now repaired: `AuthorityLibraryService` blocks public list/detail/span access without a verified active build, and `AuthorityProductService` reads list/span/lineage/opinion/rule paths from build-local hash-checked parsed artifacts. A mutable `UNADMITTED-CANARY` is rejected through both canonical and desktop APIs. Regression after repair: **1,787 passed, 22 explicitly skipped, 0 failed, 0 errors**; frozen candidate `1839c427…49ce4d8` passed offline qualification and the durable/exhibit/courtroom fictional journeys. This is not real-law, model-quality, signing, WACK, isolated-install, Store, or Enterprise certification.

- **Slice 30 metadata hardening (2026-08-27):** regression tests reproduced false-clear coverage results for parser-failed, pending, blank, and unrecognized freshness states, plus substring-only source-class matches. Coverage now requires explicitly fresh metadata and exact known class aliases, processes rows without duplicating the corpus in memory, and exposes unknown states as review blockers. Both API hosts exercise the real detector/service using fictional metadata; canonical role/tenant denial and audit, source-text/path exclusion, route uniqueness, production asset mirrors, and the retained source-browser entry are covered. The production UI now explains that the optional label does not filter the corpus. Focused regression: **22 passed, 0 failed, 0 skipped**, with one upstream test-client deprecation warning; Python compilation, both production JavaScript syntax checks, scoped lint, and diff checks passed. This is source/API and static production-asset evidence, not browser interaction, rebuilt frozen/MSIX reachability, real-authority, or Enterprise certification. Evidence: `dist/ga_today/evidence/heartbeat_slice30_metadata_hardening.json`.

- **Wave 22 source-regression checkpoint (2026-08-27):** the canonical production gateway, mirrored production assets, and source tree were revalidated after the current hardening work. Python compilation, both production JavaScript syntax checks, test collection, and a full `pytest -q` run passed with **1,722 passed, 22 explicitly skipped, 0 failed, and 0 errors**. Six skips require unavailable archived GA evidence; the other skips are optional native-audio or unavailable Windows symlink/POSIX-mode host capabilities. This is source-checkout evidence only—not frozen-runtime, MSIX, Store, Enterprise, attorney-review, pilot, or human-signoff evidence. Slice 23 remains blocked until an admitted, licensed local cross-encoder artifact, hardware plan, and evaluation evidence are supplied.
- **Wave 23 full frozen-runtime checkpoint (2026-08-27):** the canonical full offline tier was rebuilt after repairing the frozen record-upload dependency and Docling artifact-path defects. Its exact frozen executable passed startup, a twelve-component bundled-engine inventory, an eighteen-check fictional offline qualification with zero external connections, and a real production-workbench browser action that failed closed when admitted authority was unavailable. The exact `8.0.0.0` MSIX passed private-data, sealed-payload, sealed-archive, package-path, and size-budget audits. It is deliberately **unsigned** and WACK requires elevation on this host, so this is not Store, Enterprise, isolated-install, attorney, pilot, or human-signoff evidence. Evidence: `dist/ga_today/evidence/wave23_full_frozen_runtime_qualification.json`.
- **Wave 24 full-package checkpoint (2026-08-27):** the exact 1.59-GB full offline `8.0.0.0` MSIX (`a5ab4ec122522690e5050f1be7acde3ce29b75944f1352bedeff9e1e65b79d9d`) passed a fresh eighteen-check fictional installed-runtime qualification. Import, OCR/document intelligence, private-record retrieval/source cards, duplicate/change handling, and privacy/redaction passed with no external network connections across 149 process-tree samples. Private-data, bundled-engine, sealed-payload/archive, and size audits passed. The package is unsigned and WACK was not run; this remains neither Store/Enterprise evidence nor a blanket certification of every roadmap slice. Evidence: `dist/ga_today/evidence/wave24_full_msix_offline_qualification.json`.
- **Wave 24 source-regression checkpoint (2026-08-27):** after the final mirrored workbench navigation hardening, Python compilation and both shipped-workbench JavaScript syntax checks passed. The fresh canonical regression completed with **1,746 tests, 0 failures, 0 errors, and 22 explicit skips** in 1,542.389 seconds. This verifies the current source checkout and the focused slice contracts; it does not upgrade the earlier MSIX evidence into universal frozen-app proof, because that package predates the final source-only pointer fallback. Evidence: `dist/ga_today/evidence/wave24_full_regression_summary.json` and `wave24_full_regression_junit.xml`.
- **Wave 25 current-package checkpoint (2026-08-27):** the full offline `8.0.0.0` MSIX was rebuilt from the current worktree after the drawer-navigation hardening. Runtime smoke, the twelve-component engine inventory, private-data, sealed-payload, sealed-archive, and size-budget audits passed. The exact 1.59-GB package (`4eaf01b896ed7c687d30f04757d8b8cbfb08a2e1de163e17663ec9dfcbfe5e92`) then passed all eighteen fictional installed-offline checks with zero observed external connections across 154 process-tree samples. Its production UI rendered the default Chat view, opened Full Workbench by pointer, and switched the Evidence drawer to Setup with keyboard activation. In-app-browser pointer automation of that tab was inconclusive, so this remains accurate frozen reachability evidence rather than a blanket native-mouse or every-slice E2E claim. The MSIX is unsigned; WACK and isolated Store installation were not run, and the archived canonical GA E2E evidence is absent—the legacy evidence generator is hard-coded to a failing v6 package and must be replaced, not replayed as v8 proof. Evidence: `dist/ga_today/evidence/wave25_current_full_package_qualification.json`.
- **Wave 27 final package checkpoint (2026-08-27):** the full offline `8.0.0.0` MSIX was rebuilt after the packaging-spec hardening that explicitly retains tracked-DOCX and record-upload runtime dependencies while avoiding false static collection of the dynamic `src` compatibility package. Runtime smoke, runtime cleanup, dependency probing, twelve-engine inventory, private-data, package-path, sealed-payload, sealed-archive, and size-budget audits passed. The exact 1.59-GB package (`6ae1fed30d64573c9edc626dc7a332c999f2132228f7900c0c555e705cfe970c`) passed all eighteen fictional installed-offline checks with zero observed external connections across 150 process-tree samples. The retired hard-coded v6 GA evidence writer now refuses execution, preventing stale results from being misrepresented as v8 proof. This qualifies only the listed local fictional/offline core workflows. It remains unsigned; WACK, isolated Store installation, and a replacement v8 canonical GA E2E matrix remain outstanding. Slice 23 still requires an externally admitted local cross-encoder artifact and evaluation evidence. Evidence: `dist/ga_today/evidence/wave27_final_clean_package_qualification.json`.
- **Wave 28 v8 core-E2E checkpoint (2026-08-27):** a current evidence assembler now refuses any unpaired runtime, failed qualification check, absent production-UI entry smoke, package-hash mismatch, failed durable-restart report, or navigation report that reused an existing profile. Against the exact Wave 27 package it recorded twelve directly verified frozen-runtime/core journeys: launch, isolated production navigation through Chat → Full workbench → Evidence Setup, fictional-matter activation, local indexing/retrieval, source-card answers, document intelligence, privacy derivative, OCR preservation, duplicate/change review, zero-network Local-only behavior, and a two-launch durable draft/revision/audit restart. Two broad journeys remain explicitly not evaluated: a current admitted-authority question and feature-by-feature workbench interaction. This is an honest `CORE_E2E_VERIFIED_WITH_LIMITATIONS` result, not full 200-slice, Store, or Enterprise certification. Evidence: `dist/ga_today/wave27_final_clean_package_build/evidence/08_v8_core_e2e_matrix.json`, `08_v8_durable_restart_e2e.json`, and `08_v8_isolated_ui_navigation.json`.
- **Wave 29 exhibit-and-media checkpoint (2026-08-27):** the exact Wave 27 frozen executable passed two zero-network, disposable-fictional-matter runs. The eleven-check exhibit run created a source-hash-bound exhibit candidate, created four unresolved review prompts with every legal-conclusion state `not_determined`, drilled into the exact source hash, recorded two confirmed custody observations, and verified the local receipt chain. The twelve-check courtroom-media run imported fictional audio, created a source-hash-bound transcript and confirmed local review session, verified local playback and keyboard control metadata, synchronized one transcript segment, verified that a fictional private note was separately encrypted and absent from the session inventory, and observed zero external connections. During live package UI inspection, the command-menu Exhibits route was found to open a generic review shell rather than the actual Exhibit workspace containing the admission and custody controls. The source and mirrored production assets now route that command to the Exhibit workspace, with focused route and syntax coverage; the current package predates that repair and must be rebuilt before frozen UI reachability can be claimed. Evidence: `dist/ga_today/wave27_final_clean_package_build/evidence/08_v8_exhibit_workbench_e2e.json` and `08_v8_courtroom_media_e2e.json`.

- **Slices 1–2:** implemented and verified in the source checkout plus local production-asset smoke. Evidence: `dist/ga_today/evidence/09_chat_streaming_performance.json`.
- **Slice 3:** implemented and verified in the source checkout plus local production-asset smoke. Evidence: `dist/ga_today/evidence/10_fast_answer_path.json`.
- **Slice 4:** implemented and verified in the source checkout plus local production-asset smoke. Evidence: `dist/ga_today/evidence/11_progressive_grounded_answers.json`.
- **Slice 5:** implemented and verified in the source checkout plus local production-asset smoke, with intentionally no-prose continuity scope. Evidence: `dist/ga_today/evidence/12_safe_conversation_context.json`.
- **Slice 6:** implemented and verified in the source checkout plus local production-asset smoke. Evidence: `dist/ga_today/evidence/13_answer_intent_router.json`.
- **Slice 7:** implemented and verified by focused service/API, production-asset, and mirror tests. The browser smoke is pending because the retained browser session is not callable in this environment. Evidence: `dist/ga_today/evidence/14_actionable_answer_footer.json`.
- **Slice 8:** the existing inline source hover/preview path was audited and hardened so a snippet without an admitted character range is explicitly marked as not an exact span. Evidence: `dist/ga_today/evidence/15_inline_source_preview.json`.
- **Slice 9:** implemented with a session- and matter-scoped immutable correction receipt, transient verifier re-run, exact source drill-down, and fail-closed matter audit. Evidence: `dist/ga_today/evidence/16_answer_correction_workflow.json`.
- **Slice 10:** implemented with bounded local latency receipts, privacy-safe audit in active matters, aggregate inspection, and in-answer timing disclosure. Evidence: `dist/ga_today/evidence/17_chat_latency_observatory.json`.
- **Slice 11:** implemented with concise, standard, and thorough display modes carrying the same source-basis receipt and review requirements. Evidence: `dist/ga_today/evidence/18_response_depth_control.json`.
- **Slice 12:** implemented with explicit audience presentation lanes that cannot alter retrieval, sources, verifier states, or legal truth. Evidence: `dist/ga_today/evidence/19_audience_aware_explanations.json`.
- **Slice 13:** implemented with one material, explanation-backed clarification question and safe workflow-selecting follow-ups. Evidence: `dist/ga_today/evidence/20_clarification_minimizer.json`.
- **Slice 14:** implemented with an explicit source-bound/user-provided/unknown assumption ledger and non-mutating correction handoff. Evidence: `dist/ga_today/evidence/21_assumption_ledger.json`.
- **Slice 15:** implemented with transient same-source comparison, side-by-side verifier states, and no automatic selection or drafting. Evidence: `dist/ga_today/evidence/22_answer_comparison.json`.
- **Slice 16:** implemented with isolated source-lineage branches that omit raw conversation and matter text. Evidence: `dist/ga_today/evidence/23_conversation_branching.json`.
- **Slice 17:** implemented with encrypted, matter-scoped fact pins, exact source locators, effective-date and dispute fields, hash-linked local audit, and a review-required chat action. Evidence: `dist/ga_today/evidence/24_pinned_conversation_facts.json`.
- **Slice 18:** implemented with compound-question decomposition, explicit independent-verification status, shared-source-basis receipts, and focused follow-up actions. Evidence: `dist/ga_today/evidence/25_question_decomposition.json`.
- **Slice 19:** implemented with a narrow, source-bound conflicting-date review prompt against encrypted pinned facts; it cannot decide truth, legal effect, or materiality. Evidence: `dist/ga_today/evidence/26_contradiction_followups.json`.
- **Slice 20:** implemented with deterministic structural answer checks and a separate human-review rubric; it never represents synthetic signals as attorney review or a correctness certification. Evidence: `dist/ga_today/evidence/27_response_usefulness.json`.
- **Frozen runtime / MSIX status for slices 1–20:** not yet evaluated. No slice is represented here as Store or Enterprise GA evidence.
- **Slice 21:** implemented in the source checkout with bounded local lexical-plus-deterministic-semantic reciprocal-rank fusion, exact-citation priority, rank explanation, and a visible lexical fallback for corpora above the interactive cap. Evidence: `dist/ga_today/evidence/28_hybrid_retrieval_rank_explainability.json`.
- **Frozen runtime / MSIX status for slice 21:** not yet evaluated. The larger-corpus lexical fallback is a disclosed usability limitation, not a semantic-retrieval claim.
- **Slice 22:** implemented with deterministic, local Maine-vocabulary expansion that preserves exact references, refuses to add Maine synonyms when a non-Maine jurisdiction is detected, and makes that boundary visible in the shipped chat UI. Evidence: `dist/ga_today/evidence/29_guarded_query_expansion.json`.
- **Frozen runtime / MSIX status for slice 22:** not yet evaluated. No slice is represented here as Store or Enterprise GA evidence.
- **Slice 23:** blocked. The repository and the current packaged runtime contain no admitted local cross-encoder/reranker model or admission record. The existing authority-priority fallback works, but is not represented as cross-encoder reranking. Evidence: `dist/ga_today/evidence/30_cross_encoder_reranking_blocker.json`.
- **Slice 24:** implemented in the source checkout with an active-authority citation-graph API and shipped source-card action. It exposes parsed links only and never claims treatment or legal effect. Evidence: `dist/ga_today/evidence/31_citation_graph_retrieval.json`.
- **Slice 25:** implemented with an explicit as-of-date metadata review lane that blocks a historical-law conclusion when returned authority is later-dated, stale, unknown, or incomplete. Evidence: `dist/ga_today/evidence/32_temporal_authority_resolver.json`.
- **Slice 26:** implemented as an honest negative-treatment review lane: every legal source card visibly carries its parsed status, defaulting to unknown where no admitted treatment evidence exists. Evidence: `dist/ga_today/evidence/33_negative_treatment_lane.json`.
- **Slice 27:** implemented with a narrow same-citation metadata conflict explainer and explicit source-version drill-down; it does not decide controlling authority or legal effect. Evidence: `dist/ga_today/evidence/34_authority_conflict_explainer.json`.
- **Slice 28:** implemented in the source checkout with exact admitted nested statute and rule offsets, Law Court paragraph/page metadata, form revision metadata, a canonical authority API, and a hidden-by-default production UI control. It never infers legal effect and reports an absent exact span honestly. Focused service/API/UI tests pass.
- **Frozen runtime / MSIX status for slice 28:** source assets and frozen-runtime route registration are verified; a rebuilt MSIX is still required before any packaged-reachability claim.
- **Slice 29:** implemented in the source checkout. Normal production retrieval now defers redundant source classes and duplicate citations where alternatives exist, while preserving admitted exact-citation hits. Its retrieval receipt explicitly says diversity is an aid, not a legal coverage conclusion. Focused tests pass.
- **Frozen runtime / MSIX status for slice 29:** not yet evaluated.
- **Slice 30:** implemented in the source checkout. The authority-gap review uses active admitted metadata only, reports missing material classes and stale/unknown freshness as review blockers, and expressly refuses to determine authority completeness or current law. It is available through the protected canonical API and hidden-by-default Authority setup panel, with a source-card drill-down action. Focused service/API/UI-asset tests pass.
- **Frozen runtime / MSIX status for slice 30:** the exact r11 MSIX browser path now exercised the read-only active-corpus review control with zero observed external connections. It retained missing-class/freshness blockers and did not determine authority completeness or current law. The generic source-library provenance defect above still blocks blanket acceptance.
- **Slice 31:** implemented in the source checkout. Source updates now stage a verified immutable build and never activate it automatically; users can inspect an admitted-source hash diff, then explicitly activate or restore a selected verified build. Activation uses an atomic pointer and durable receipt. An interrupted or nonqualifying update leaves the active build unchanged. Focused service/API/UI-asset and frozen-route tests pass.
- **Frozen runtime / MSIX status for slice 31:** the exact r11 MSIX passed a fictional browser-driven stage → hash-diff review → explicit activation → restart persistence → explicit rollback path. The separate external-root browser check did not mutate authority data. This is not live-source-update, signing, Store, or Enterprise evidence.
- **Slice 32:** implemented in the source checkout. A verified authority-generation diff can now map exact source overlap across saved documents and draft types, form contexts, prior review packets, and encrypted calendar-rule source hashes. The feature never treats a hash change as a legal, form, or deadline conclusion; raw saved research is intentionally not retained and is disclosed as unavailable rather than guessed. Matter-impact accesses write an encrypted, append-only no-prose audit receipt and the canonical API binds every operation to the active matter plus global role and tenant enforcement. Focused service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass32_authority_change_impact_vertical.json`.
- **Frozen runtime / MSIX status for slice 32:** the exact r11 MSIX now passed a browser-driven fictional journey: selected reviewed document, immutable generation selection, exact-document source-overlap analysis, whole-matter recheck inventory, explicit packet approval, and opaque packet-artifact links. A changed source invalidated prior approval and remained review-required; saved research was disclosed as intentionally unpersisted. The disposable external authority root generated zero observed external connections and was removed on exit. This is not live-source, signing, Store, or Enterprise evidence. Evidence: `dist/ga_today/evidence/08_v8_authority_impact_ui_r11.json`.
- **Slice 33:** implemented in the source checkout. The authority freshness dashboard shows class-specific operational review intervals, overdue metadata, missing/invalid retrieval dates, parser failures, and the last accepted build. It makes no network request, changes no authority state, and never treats an operational interval as a conclusion that law is current or complete. The protected canonical API, frozen source route, and hidden-by-default production Authority setup panel provide exact-source drill-down with visible review-required status. Focused service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass33_freshness_dashboard.json`.
- **Frozen runtime / MSIX status for slice 33:** the exact r11 MSIX browser UI displayed the read-only freshness review for the active immutable build with visible review-required boundaries and zero observed external connections. It is not a live-currentness conclusion.
- **Slice 34:** implemented in the source checkout. The availability monitor reads admitted fetch/update metadata only, surfaces moved official URLs, changed hashes, stored TLS/certificate failures, and access restrictions, and never requests a source, accepts a redirect, or substitutes a mirror. The protected canonical API, frozen source route, and hidden-by-default Authority setup panel provide exact-source drill-down and visible review-required status. Focused service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass34_official_source_availability_monitor.json`.
- **Frozen runtime / MSIX status for slice 34:** the exact r11 MSIX browser UI displayed stored availability metadata without a source request, redirect, or mirror substitution. It retained visible review-required status; it is not a live availability check.
- **Slice 35:** implemented in the source checkout. A versioned, hash-bound synthetic page-shape corpus now exercises title-index layouts, forms and opinions tables, rule-text footnotes, a missing required heading, and a malformed download that is quarantined before parser invocation. The protected canonical API and hidden-by-default Authority setup panel expose only fixture receipts and hashes; the corpus is explicitly unavailable for authority search or legal claims. Focused parser/service/API/UI-asset tests pass, and the frozen source API executes the bundled fixture path. Evidence: `dist/ga_today/evidence/pass35_parser_regression_corpus.json`.
- **Frozen runtime / MSIX status for slice 35:** the exact r11 MSIX browser UI displayed the bundled synthetic parser-regression receipt (6/6) and preserved the synthetic-fixture boundary. It does not prove an external parser corpus or legal accuracy.
- **Slice 36:** implemented in the source checkout. The authority lineage inspector traces one admitted parsed node to its materialized snapshot, recorded retrieval metadata, official URL, and immutable build fingerprint. It performs no network work, does not infer current law or legal effect, visibly remains review-required, and fails closed when the parsed record hash does not match the active snapshot. The protected canonical API writes a global audit receipt; a hidden-by-default Authority setup control has an exact-source drill-down. Focused service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass36_authority_lineage_inspector.json`.
- **Frozen runtime / MSIX status for slice 36:** the exact r11 MSIX browser UI performed an admitted-source lineage drill-down to the active immutable build, parsed-node offsets, snapshot hash, and recorded metadata without fetching a source. Review-required status remained visible.
- **Slice 37:** implemented in the source checkout. The installed-form catalog check accepts only a form ID plus version date and/or SHA-256, compares those metadata fields against the active admitted form catalog, and blocks a completion path for stale, unknown, missing, mismatched, or conflicting catalog metadata. It never reads form contents, persists supplied metadata, fetches a source, or treats a catalog match as filing approval. The protected canonical API writes a global audit receipt; the hidden-by-default Authority setup control provides an admitted-source drill-down. Focused service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass37_forms_catalog_synchronizer.json`.
- **Frozen runtime / MSIX status for slice 37:** the exact r11 MSIX browser UI compared a fictional metadata submission to the admitted catalog and correctly blocked completion on a metadata conflict. It did not read form contents or imply filing approval.
- **Slice 38:** implemented in the source checkout. The Law Court opinion inspector exposes admitted opinion metadata, deterministic disposition signals, exact numbered-paragraph spans, cited-authority locations, and a bounded exact-source excerpt. Missing paragraph or disposition spans block a clean inspection; no output determines a holding, treatment, current law, precedential weight, or outcome. The protected canonical API writes a global audit receipt; the hidden-by-default Authority setup control has exact-source drill-down. Focused service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass38_law_court_opinion_enrichment.json`.
- **Frozen runtime / MSIX status for slice 38:** the exact r11 MSIX browser UI inspected admitted opinion metadata and kept unavailable disposition, paragraph, and text spans as visible blockers. It made no holding, treatment, or current-law inference.
- **Slice 39:** implemented in the source checkout. Rule parsing now retains only explicitly printed effective dates and amendment events. The rule-history inspector displays their exact admitted spans, source/build hashes, and official URLs while blocking a clean timeline when the history is absent, ambiguous, or not tied to the active snapshot. It never decides which version applies, supersession, or legal effect. The protected canonical API writes a global audit receipt; the hidden-by-default Authority setup control has exact-source drill-down. Focused parser/service/API/UI-asset and frozen-route tests pass. Evidence: `dist/ga_today/evidence/pass39_rule_history_timeline.json`.
- **Frozen runtime / MSIX status for slice 39:** the exact r11 MSIX browser UI inspected admitted rule metadata and kept unavailable effective/amendment events as visible blockers. It did not infer the applicable version or legal effect.
- **Slice 40:** implemented in the source checkout and production gateway. A signed authority bundle can be exported as a deterministic portable archive only after existing signature and hash verification; an explicitly selected local archive is bounded, path-safe extracted, reverified, and atomically activated. Duplicate members, path traversal, archive-size abuse, untrusted signatures, and rollback are rejected. The UI requires an explicit local administrator acknowledgement and never exposes a raw path, creates a signing key, downloads authority, or adds authority data to the MSIX. Protected administrator operations enforce tenant scope and emit audit receipts. Focused service/gateway/UI-asset tests pass. Evidence: `dist/ga_today/evidence/pass40_offline_authority_portability.json`.
- **Frozen runtime / MSIX status for slice 40:** the exact r11 MSIX browser UI displayed signed-bundle status with no bundle available and a visible review-required state. Export/import/activation were deliberately not exercised against the external authority root; package inclusion remains prohibited.
- **Slice 41:** implemented in the source checkout. Timeline-event corrections now validate every new binding against an active-matter record and its SHA-256; a caller cannot substitute a source hash or reference a record outside the matter. Corrections preserve the prior date and source binding in append-only history, remain review-required, and expose an ephemeral, verified local-record capability only after the stored source hash still matches. The production workbench’s Evidence drawer provides a correction, history, rebinding, and source-inspection path without exposing paths or altering the underlying record. Focused API/service/UI-asset tests pass. Evidence: `dist/ga_today/evidence/pass41_timeline_correction_acceptance.json`.
- **Frozen runtime / MSIX status for slice 41:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The Evidence drawer loaded a hash-bound event, recorded a review-required date/source correction in append-only history, and opened its verified local source inspector without a raw-path disclosure or external connection. This is not live-matter, legal-quality, isolated Store-installation, signing, WACK, attorney-review, Enterprise, or GA proof. Evidence: `dist/ga_today/evidence/08_v8_timeline_correction_ui_r11.json`.
- **Slice 42:** implemented in the source checkout. Claim review now requires one or more active-matter records, rejects unknown selections, produces source-bound candidate support, contradiction, qualification, alternative-context, reliability, and missing-context cards, and presents an explicit non-conclusive automated disposition. Reviewer decisions are allowlisted, append-only, and remain review-required; a `filing_ready` decision cannot be supplied. A source card may open the local record inspector only after its current SHA-256 still matches the active-matter record. The production Evidence drawer supports creation, review, history, decision recording, and exact-source inspection. Focused API/service/UI-asset tests pass. Evidence: `dist/ga_today/evidence/pass42_claim_disposition_acceptance.json`.
- **Frozen runtime / MSIX status for slice 42:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The Evidence drawer created a source-bound claim review, displayed candidate-support cards plus reliability/unresolved-source blockers, opened an exact local source without a raw-path disclosure, and recorded an allowed reviewer decision while preserving review-required status. This is not a factual finding, legal-quality, isolated Store-installation, signing, WACK, attorney-review, Enterprise, or GA result. Evidence: `dist/ga_today/evidence/08_v8_claim_disposition_ui_r11.json`.
- **Slice 43:** implemented in the source checkout. The whole-matter command center now aggregates operational review blockers from snapshot state, packet availability/review, hash coverage, privacy flags, and parser/OCR signals; every blocker identifies a scope-limited corrective action. Health observations are append-only and hash-chained, deduplicated when state is unchanged, and fail closed on tampering. Record-level actions request a fresh local capability only for an active-matter record with a valid source hash. The existing production command-center overlay now shows blockers, exact actions, health history, and guarded record inspection. Focused API/service/UI-asset tests pass. Evidence: `dist/ga_today/evidence/pass43_command_center_acceptance.json`.
- **Frozen runtime / MSIX status for slice 43:** r11 exposed a real reachability defect: its command-center overlay had no command-palette or view-menu entry. Source and mirror were repaired with an `Open whole-matter command center` command and focused tests pass, but the exact r11 package predates that repair. A rebuilt MSIX and frozen-runtime browser journey remain required before this slice can be marked package-reachable. Evidence: `dist/ga_today/evidence/08_command_center_reachability_r11_prepackage.json`.
- **Slice 44:** implemented in the source checkout. Attachment coverage now stores only explicitly documented, source-bound items in the active matter and distinguishes alleged, referenced, expected, not-yet-reviewed, located, and absent-in-selected-scope states. “Absent” is expressly scoped and never represented as proof of nonexistence elsewhere. State changes are append-only, review-required, and fail closed if a referenced record is foreign, a located item lacks its local record, or the original source hash has changed. The production Evidence drawer exposes creation, state review, coverage counts, history, and exact-source inspection. Focused API/service/UI-asset tests pass. Evidence: `dist/ga_today/evidence/pass44_attachment_coverage_acceptance.json`.
- **Frozen runtime / MSIX status for slice 44:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The production Evidence drawer created a source-bound coverage item, opened the hash-verified source without a raw filesystem-path disclosure, and recorded an append-only `absent_in_selected_scope` review while keeping the scope caveat and review-required status visible. The temporary fixture automatically tore down with zero observed external connections. This proves the existing r11 executable path, not a fresh rebuild from the current dirty source tree, isolated Store installation, signing, WACK, attorney-review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_attachment_coverage_ui_r11.json`.
- **Slice 45:** implemented in the source checkout. The canonical matter fact graph now accepts only source-bound active-matter nodes and relationships for people, events, orders, assertions, records, and sources. Every node and edge preserves an explicit unknown, disputed, observed, alleged, or not-yet-reviewed state and rejects missing graph endpoints, foreign source records, invalid relationship types, and changed-source drill-down. The production Evidence drawer supports meaningful node and relationship creation, graph refresh, and source inspection without presenting the graph as a fact finder. Focused API/service/UI-asset tests and a duplicate-route scan pass. Evidence: `dist/ga_today/evidence/pass45_fact_graph_acceptance.json`.
- **Frozen runtime / MSIX status for slice 45:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The Evidence drawer created two source-bound nodes with explicit observed/disputed states, recorded a source-bound disputed contradiction relationship, and opened its hash-verified local source without a raw filesystem-path disclosure. Graph relationships remained visibly review-required and explicitly non-conclusive; the temporary fixture tore down with zero observed external connections. This proves the existing r11 executable path, not a fresh rebuild from the current dirty source tree, isolated Store installation, signing, WACK, attorney-review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_fact_graph_ui_r11.json`.
- **Slice 46:** implemented in the source checkout. The issue-to-proof matrix records an active-matter source-bound row for a stated issue, proof item, candidate support, contradiction, qualification, or explicitly missing proof. Optional authority strings remain unverified candidates and cannot be turned into a legal-element, fact, sufficiency, jurisdiction, or filing-readiness determination. Each row has append-only review history, a current-source-hash check, a guarded local source inspector, and a visible review-required state. The production Evidence drawer supports creation, review-state recording, rollup by issue, and source inspection. Focused API/service/UI-asset tests and a duplicate-route scan pass. Evidence: `dist/ga_today/evidence/pass46_issue_proof_matrix_acceptance.json`.
- **Frozen runtime / MSIX status for slice 46:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The Evidence drawer created a source-bound issue-to-proof qualification row, exposed the supplied authority as an unverified candidate, opened its hash-verified local source without a raw filesystem-path disclosure, and recorded an append-only qualification review while retaining the non-conclusive review-required boundary. The temporary fixture tore down with zero observed external connections. This proves the existing r11 executable path, not a fresh rebuild from the current dirty source tree, isolated Store installation, signing, WACK, attorney-review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_issue_proof_matrix_ui_r11.json`.
- **Slice 47:** implemented in the source checkout. Matter-change digest checkpoints record an active-matter record-hash and review-work baseline. A later digest exposes new, changed, unavailable, review-work, candidate-contradiction, and calendar-metadata items that need human review, with guarded source inspection for current affected records. It is expressly not an altered-conclusion detector, contradiction resolver, deadline calculator, legal-effect determination, or approval path. The production Evidence drawer supports checkpoint creation, digest generation, and source inspection. Focused API/service/UI-asset tests pass, including duplicate/unknown-checkpoint failure handling; missing review objects now return stable unquoted recovery codes. Evidence: `dist/ga_today/evidence/pass47_matter_change_digest_acceptance.json`.
- **Frozen runtime / MSIX status for slice 47:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The Evidence drawer created a review-required baseline, detected one atomically added fictional record after that baseline, rendered it as a record to inspect without inferring an altered conclusion, and opened its hash-verified local source without a raw filesystem-path disclosure. The temporary fixture tore down with zero observed external connections. This proves the existing r11 executable path, not a fresh rebuild from the current dirty source tree, isolated Store installation, signing, WACK, attorney-review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_matter_change_digest_ui_r11.json`.
- **Slice 48:** implemented in the source checkout. Record-lineage links now connect two distinct active-matter records as a review-only exact duplicate, changed copy, OCR correction, translation, redaction, export derivative, or derived copy. Both ends retain their own source hashes and may be inspected only after current-hash validation. A link never changes or deletes an original record, and it does not decide authenticity, admissibility, completeness, legal effect, or which text controls. The production Evidence drawer supports creation, refresh, and guarded inspection of both sources. Focused API/service/UI-asset tests pass. Evidence: `dist/ga_today/evidence/pass48_record_lineage_acceptance.json`.
- **Frozen runtime / MSIX status for slice 48:** the exact r11 MSIX frozen runtime was browser-exercised with a disposable fictional matter. The Evidence drawer created a source-bound changed-copy provenance proposal between two active-matter records, kept its review-required/non-determinative boundary visible, and inspected both original and related hash-verified sources without raw filesystem-path disclosure. The temporary fixture tore down with zero observed external connections. This proves the existing r11 executable path, not a fresh rebuild from the current dirty source tree, isolated Store installation, signing, WACK, attorney-review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_record_lineage_ui_r11.json`.
- **Slice 49:** implemented in the source checkout. Cross-document entity candidates require two distinct active-matter records, stay review-required, and cannot be confirmed by a guess or inferred match. An explicit human confirmation creates a reversible logical merge receipt without modifying either original record; a revocation restores the candidate state. Focused API/service/UI-asset tests pass.
- **Frozen runtime / MSIX status for slice 49:** a fresh unsigned/offline essential-tier QA MSIX built from the current source was browser-exercised with a disposable fictional matter. The production Evidence drawer created a source-bound identity candidate, visibly recorded explicit confirmation as a reversible logical merge, visibly revoked that merge, and opened a hash-verified private source preview without a raw filesystem-path disclosure. The fixture closed with zero observed external connections across 3,767 samples and no browser warnings/errors. This proves the fresh frozen-runtime UI path only—not isolated installation, signing, Store submission, attorney review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_entity_resolution_ui_current_qa.json`.
- **Slice 50:** implemented in the source checkout. Matter completeness now exposes explainable operational coverage dimensions and blockers for record inventory, dates, parser/OCR review, missing-record review, and issue-to-proof work. It explicitly does not score a case, predict an outcome, assess legal sufficiency, or approve filing. Focused API/service/UI-asset tests pass.
- **Frozen runtime / MSIX status for slice 50:** the fresh unsigned/offline essential-tier QA MSIX was browser-exercised with a disposable fictional matter. The production Evidence drawer exposed record inventory, date review, source integrity, missing-record review, and issue-to-proof review dimensions, including a visible undated-record review blocker and an explicit non-scoring/non-filing boundary. The fixture closed with zero observed external connections across 2,778 samples and no browser warnings/errors. This proves the fresh frozen-runtime UI path only—not isolated installation, signing, Store submission, attorney review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_v8_matter_completeness_ui_current_qa.json`.
- **Slice 51:** acceptance coverage verified the existing shipped local-OCR workflow: explicit consent, active-matter binding, durable local job state, resume/status contract, responsive progress, cancellation, and no document-byte/text egress. It reuses the canonical pipeline rather than introducing a parallel importer. Focused source/API/UI-contract and runtime-kernel tests pass.
- **Frozen runtime / MSIX status for slice 51:** a fresh unsigned/offline essential-tier QA MSIX built from the current source was exercised with a disposable fictional image-only PDF. The production UI required affirmative local-OCR consent, exposed the local-engine and privacy boundary, produced review-visible progress, created an OCR-derived source card, and opened a hash-verified protected source preview with no raw filesystem-path disclosure. A second run against the exact fresh runtime proved canonical cancellation: queued → cancelling → cancelled, no derived page was indexed, the scan remained pending for retry, originals were unchanged, and the fixture closed with zero observed external connections. This proves bounded frozen-runtime reachability only—not isolated installation, signing, Store submission, attorney review, Enterprise, or GA. Evidence: `dist/ga_today/evidence/08_slice51_incremental_ingest_release_qa.json`.
- **Slice 52:** implemented as an opt-in, one-shot local candidate scan. It requires an active matter and an explicit local folder choice, returns only bounded candidate metadata, never starts a hidden watcher or imports a file, and keeps the folder path out of the response. Focused API/service/UI-asset tests pass.
- **Frozen runtime / MSIX status for slice 52 (current essential QA candidate):** the exact unsigned/offline `8.0.0.0` package browser-exercised the production Evidence drawer with a fictional active matter and public package assets. A drawer re-render defect that could leave the visible scan button without a listener was repaired with one document-level delegated handler. The user action completed a bounded explicit scan, kept the folder path out of the response, displayed the review-required/no-background-watch boundary, emitted no browser warnings or errors, and the disposable fixture closed with zero observed external connections across 7,786 samples. Package privacy, sealed-payload, sealed-archive, and essential-tier size audits passed. This proves only the listed frozen-runtime interaction; it is not isolated-installation, signing, WACK, Store, attorney, Enterprise, or GA evidence. Evidence: `dist/ga_today/evidence/08_slice52_watch_folder_frozen_ui.json`.
- **Slice 53:** implemented as a source-hash-bound scanner cleanup review plan. Duplex, blank-page, and rotation annotations remain proposed review items; originals are not changed, deleted, rotated, or OCR'd by this path. Focused API/service/UI-asset tests pass.
- **Frozen runtime / MSIX status for slice 53 (current essential QA candidate):** the exact unsigned/offline `8.0.0.0` package browser-exercised the production Evidence drawer with a disposable fictional image-only PDF. The route now rejects a syntactically valid hash unless it belongs to a current active-matter record, records a minimal append-only review receipt, and exposes a guarded exact-source action. A valid plan visibly retained review-required status and the immutable-original boundary, then opened the verified local source inspector without a raw path. The fixture closed with zero observed external connections across 6,683 samples and no browser warnings or errors. Package privacy, sealed-payload, sealed-archive, and essential-tier size audits passed. This proves only the listed frozen-runtime interaction; it is not isolated-installation, signing, WACK, Store, attorney, Enterprise, or GA evidence. Evidence: `dist/ga_today/evidence/08_slice53_scanner_review_frozen_ui.json`.
- **Slice 54:** implemented as a bounded, source-hash-bound handwriting-review routing lane. It records an uncertain-OCR or possible-handwriting signal for human transcription review and never claims handwriting recognition or generates text. The canonical route now rejects foreign/invented hashes, persists a minimal append-only review receipt, and the production UI exposes guarded exact-source inspection. Focused API/service/UI-asset tests pass.
- **Slice 55:** implemented as a review-only document-type candidate lane using a current active-matter source hash and bounded exact record excerpt. The stored receipt retains only source identity plus excerpt hash/length and candidate signals; suggested labels do not alter record metadata or decide legal effect. Focused API/service/UI-asset tests pass.
- **Slice 56:** implemented as a current active-matter, source-hash-bound page-quality review map over supplied parser/OCR metrics. It rejects out-of-range source pages, stores only a bounded metrics receipt, and exposes review-required page flags without replacing image review or creating a quality determination. Focused API/service/UI-asset tests pass.
- **Slice 57:** implemented as a current active-matter, source-hash-bound table-cell lineage inspector. Each supplied cell retains page coordinates and OCR text in the immediate review response, while persisted history keeps only bounded hashes and counts; corrections are explicitly separate from original extraction. Focused API/service/UI-asset tests pass.
- **Slice 58:** implemented in the source checkout as an encrypted, matter-scoped document-comparison receipt. It compares admitted local text extracts, record metadata, available table/signature counts, and page-image derivative hashes; missing inputs remain visible blockers. It preserves both source hashes, writes a hash-linked audit event, opens exact sources only after revalidating their current hashes, and does not determine authenticity, controlling text, or legal effect. Focused API/service/UI-asset tests pass.
- **Slice 59:** implemented in the source checkout as encrypted, active-matter batch metadata sidecars. A reviewer can apply bounded labels, a date, a safe custodian ID, confidentiality, and document type to selected active-matter records without changing an original record, source hash, or parser output. Every batch is hash-linked in append-only history and exact source access revalidates the current source hash. Focused API/service/UI-asset tests pass.
- **Slice 60:** implemented in the source checkout as encrypted, matter-specific import-policy profiles that can only tighten the global size, extension, privacy-scan, quarantine, symlink, and local-OCR-review floor. The active policy is enforced in the canonical corpus builder before a candidate is hashed, parsed, or staged; a disallowed file is recorded in the existing quarantine output rather than imported. Focused API/service/UI-asset and builder-integration tests pass.
- **Slice 61:** implemented in the source checkout as a local-media transcript-correction lane. A reviewer can record a timestamped correction proposal bound to a selected media item, transcript, and segment without changing the original transcript text or media. The correction state is encrypted, the audit receipt is hash-linked, the protected canonical route requires a reviewer-capable role, and the hidden-by-default production Evidence drawer shows the review-required source span and local-media binding. Focused service/API/UI-asset tests pass.
- **Slice 62:** implemented in the source checkout as an explicit human-confirmed speaker-label review lane. Each requested timestamped segment is validated before any label is written; unknown or incomplete selections fail without a partial update. Labels retain their source binding and text hash, remain review-required, and visibly state that no biometric identity, emotion, credibility, or deception inference occurred. The protected canonical API records the action in encrypted active-matter state with a hash-linked audit trail, and the hidden-by-default production Evidence drawer provides the same confirmed-label and source-binding path. Focused service/API/UI-asset tests pass.
- **Slice 63:** implemented in the source checkout as a source-hash-bound, local video keyframe review lane. The reviewer selects a regular video file inside the active matter; path escape, symlinks, unsupported types, oversized files, and source-hash changes fail closed before decoding. An admitted local OpenCV decoder generates bounded PNG keyframes at explicit timestamps, encrypts every derivative at rest, and returns each frame only through its protected local review route after hash verification. Human annotations require confirmation, retain their visual hash and timestamp binding, and do not determine authenticity. The hidden-by-default production Evidence drawer provides generation, preview, annotation, and exact artifact drill-down. Focused service/API/UI-asset tests pass.
- **Slice 64:** implemented in the source checkout as an explicitly confirmed, source-hash-bound local media-redaction lane. It creates encrypted review-required WAV mute derivatives and AVI blur derivatives from user-selected exact intervals and normalized regions; it performs no speaker, face, identity, emotion, or content detection. Path escape, symlinks, unsupported types, over-limit outputs, source-hash changes, and unavailable decoders/encoders fail closed. Each derivative has a source hash, derivative hash, immutable operation receipt, protected local preview, and no-original-modified guarantee. The hidden-by-default production Evidence drawer exposes this exact path. Focused audio/video service/API/UI-asset tests pass.
- **Slice 65:** implemented in the source checkout as an encrypted, active-matter screenshot-conversation reconstruction lane. It orders human-supplied screenshot observations by visible timestamp and bounded order metadata, preserving missing/unparseable timestamps, duplicate hashes, and large visible-time gaps as uncertainty rather than inferring omitted messages. Each observation retains its source hash and reviewer note, can be inspected through a protected source-binding route, and never authenticates a screenshot, identifies a sender, or concludes that a conversation is complete. The hidden-by-default production Evidence drawer provides entry, ordering, gap disclosure, and exact-source inspection. Focused service/API/UI-asset tests pass.
- **Slice 66:** implemented in the source checkout as a source-hash-bound local media-metadata inspector for imported images, WAV audio, and video. It presents available image EXIF fields (with exact GPS values withheld), WAV technical fields, or decoder-visible video container fields; makes absence and user-claimed conflicts visible; and lists existing keyframe/redaction derivatives. It never treats metadata as authentication or proof of an event. The encrypted inspection receipt is active-matter scoped and source-drillable through the protected canonical API and hidden-by-default production Evidence drawer. Focused image EXIF/API/UI-asset tests pass.
- **Slice 67:** implemented in the source checkout by extending the active-matter fact graph with temporal-before/after, attachment, reply, duplicate, contradiction, and derivative relationships. Every edge remains a source-hash-bound reviewer record, retains a relationship basis and note, has an exact source inspector, and never becomes a factual or legal finding. Unknown relationship types and foreign record bindings fail closed. The hidden-by-default production Evidence drawer exposes the v2 relationship control. Focused service/API/UI-asset tests pass.
- **Frozen runtime / MSIX status for slice 54 (current essential QA candidate):** the exact unsigned/offline `8.0.0.0` package browser-exercised the production Evidence drawer with a disposable fictional image-only PDF. A source-bound possible-handwriting signal created a review-required human-transcription routing receipt, expressly did not recognize handwriting or create a transcript, and opened the hash-verified source inspector without raw-path disclosure. The fixture closed with zero observed external connections across 3,949 samples and no browser warnings/errors. Package privacy, bundled-engine, sealed-payload, sealed-archive, and essential-tier size audits passed. This proves only the listed frozen-runtime interaction; it is not isolated-installation, signing, WACK, Store, attorney, Enterprise, transcription-quality, or GA evidence. Evidence: `dist/ga_today/evidence/08_slice54_handwriting_review_frozen_ui.json`.
- **Frozen runtime / MSIX status for slices 55–60 (current essential QA candidate):** the exact unsigned/offline `8.0.0.0` package hash `ad783aac618370a9f071a8b49da957ddde56373c831f00676da4ceb453088b9c` passed six browser-driven production Evidence-workspace actions in one disposable fictional matter. It created source-bound document-type, page-quality, table-lineage, document-comparison, metadata-sidecar, and strict import-policy receipts; retained review-required and original-preservation boundaries; opened a hash-verified DOCX source without raw-path disclosure; rendered Both and Child Impact Lens defaults; emitted no browser warnings/errors; and closed with zero observed external connections across 8,476 samples. Package privacy, engine, sealed-payload/archive, and size audits passed. This is not isolated installation, signing, WACK, Store, attorney, pilot, Enterprise, or GA proof. Evidence: `dist/ga_today/evidence/08_slices55_60_evidence_review_frozen_ui.json`.
- **Frozen runtime / MSIX status for slices 61–67:** the exact r2 essential-tier frozen executable was browser-exercised with disposable fictional media. Meaningful actions covered transcript correction, explicit speaker labeling, actual AVI keyframe generation/image preview, actual WAV mute derivative/audio player, screenshot-sequence uncertainty, JPEG EXIF/date conflict, and a source-bound graph contradiction edge. All retained review-required/source-hash/original-preservation boundaries. Both holders shut down with zero observed external connections (11,073 and 12,164 samples); browser warnings/errors were zero. The 61–70 focused group passed 30 tests. This is not every-codec, authenticity, legal, isolated-install, Store, WACK, attorney, pilot, Enterprise, or GA evidence. Evidence: `dist/ga_today/evidence/08_slices61_67_media_evidence_frozen_ui.json`.
- **Frozen runtime / MSIX status for slices 68–69 (current v8 candidate):** the exact `8.0.0.0` package hash `dbcac1ccb3a8e0959d5f663083668e373628e7b36dfc7c65cbd91d0e98dbabac` passed a disposable-fictional-matter frozen-runtime journey: corpus activation, source-hash-bound exhibit candidate, four unresolved admission-preparation prompts, exact source inspection, two user-confirmed custody events, receipt-chain verification, visible review-required state, and zero observed external TCP connections across 370 samples. This proves only the listed local software workflow; it does not determine authenticity, foundation, admissibility, legal authority, attorney review, Store qualification, or Enterprise GA. Evidence: `dist/ga_today/evidence/09_v8_exhibit_workbench_e2e_current_package.json`.
- **Frozen runtime / MSIX status for slice 70 (current v8 candidate, audio subset):** the same package passed a disposable-fictional-audio journey: import, transcript creation, confirmed source-hash-bound review session, local WAV playback payload, keyboard-control metadata, transcript synchronization, separately encrypted private note, and zero observed external TCP connections across 228 samples. Video, browser interaction, and device-level playback remain unproven. Evidence: `dist/ga_today/evidence/10_v8_courtroom_media_e2e_current_package.json`.
- **Cross-cutting durable-document status (current v8 candidate):** the same package restarted twice against one isolated application-data directory, reopened the exact fictional draft/revision, preserved the original revision and review-required state, and verified the append-only document audit chain. This is frozen-runtime canonical-API evidence, not an installed-package, OS-level network, or native-UI-close proof. Evidence: `dist/ga_today/evidence/11_v8_durable_restart_e2e_current_package.json`.
- **Frozen runtime / MSIX status for slice 71 (tracked-DOCX, repaired current candidate):** the exact `8.0.0.0` package hash `94fde41349ff179455855c626219c2bc4ef385a7fd8a3c36bf5c543366977f73` passed all thirteen checks using a disposable fictional matter: source-hash-bound DOCX import, exact paragraph inspection, explicitly confirmed tracked replacement and comment, semantic verification of the generated Word text, original preservation, review-required/non-filing state, receipt-hash match, append-only audit verification, and zero observed external TCP connections across 735 samples. The artifact download was accepted for its originating local session and rejected from a separate session; this is a session-bound boundary, not an unsupported one-time-download claim. This is frozen-runtime canonical-API evidence only; native UI interaction, isolated MSIX installation, and Store qualification remain unproven. Evidence: `dist/ga_today/evidence/12_v8_tracked_docx_e2e_repaired_package_v2.json`.
- **Frozen runtime / MSIX status for slice 72 (structured draft outline, current candidate):** the same exact package passed all twelve checks using a disposable fictional matter plus the separately stored official-authority source `statute-title-index-10a368bd5a9b`. It created an encrypted, review-required/non-filing outline with separate private-record and authority lanes; verified both source drill-downs and hashes; rejected cross-matter retrieval; confirmed the external authority store was excluded from the MSIX; and observed zero external TCP connections across 374 samples. The authority manifest records a `known_extracted_timestamp` source retrieved `2026-08-07`; this run does not claim current law. Native UI interaction, isolated MSIX installation, and Store qualification remain unproven. Evidence: `dist/ga_today/evidence/13_v8_structured_draft_outline_e2e_current_package.json`.
- **Frozen runtime / MSIX status for slice 73 (sentence-level support map, current candidate):** the same exact package passed all twelve checks using a disposable fictional draft and the separately stored official-authority source `statute-title-index-10a368bd5a9b`. It created a revision-bound encrypted map with one private-record support, an exact private-source drill-down, visible review-required/non-filing state, cross-matter denial, and zero observed external TCP connections across 163 samples. Because the selected title-index source has no exact span, the legal-like sentence remained a missing-context signal rather than becoming purported legal support; this run does not assert current law. The external authority root was excluded from the MSIX. Native UI interaction, isolated MSIX installation, and Store qualification remain unproven. Evidence: `dist/ga_today/evidence/14_v8_sentence_support_map_e2e_current_package_v2.json`.
- **Frozen runtime / authority capability status for slices 74–75 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed the frozen canonical-API citation and quote workflow against the separately stored active authority build. A hash-bound statute source exposed fourteen admitted pinpoint candidates; the journey required a reviewer selection, rejected an invalid authority identifier, created review-required/non-filing citation and exact-quote proposals, preserved both original fictional drafts, denied cross-matter receipt access, and verified zero observed external TCP connections across 344 process-tree samples. The external authority root remained excluded from the MSIX. This proves this one source-bound drafting workflow only; it does not determine current law, make a legal conclusion, prove browser/native UI interaction, isolated installation, Store qualification, or Enterprise GA. Evidence: `dist/ga_today/evidence/08_v8_source_bound_drafting_e2e_r6_current_authority.json`; focused source-checkout regression: **11 passed, 0 failed, 0 errors, 0 skipped** in `08_v8_source_bound_drafting_focused_r6.xml`.
- **Slice 73 authority-selection hardening (2026-08-29):** the sentence-level support-map API now follows the same canonical authority re-resolution boundary as citation and quote proposals. It replaces client-supplied authority text, citation, freshness, and span fields with the locally resolved source candidate; a mismatched hash returns `409 sentence_support_authority_hash_mismatch`. The focused sentence-map/citation/quote suite passed **15 tests, 0 failures, 0 errors, 0 skipped**. This repair is source-checkout evidence only; the current r6 package predates it and must be rebuilt before frozen-runtime reachability is asserted. Evidence: `dist/ga_today/evidence/08_v8_drafting_authority_boundary_regression.xml`.
- **Next direct evidence target:** exercise sentence-level support mapping against the current active build's exact source candidate, preserving the existing fail-closed behavior for any stale, missing, or wrong-jurisdiction metadata.

- **Source-bound drafting boundary follow-up (2026-08-29):** the next frozen candidates exposed two real exact-source matching defects: citation abbreviations such as `M.R.S.` were being split as sentence endings, and a substantive selected-authority sentence without its own citation label was classified as factual. The canonical matcher now preserves common legal abbreviations while splitting, and classifies a sentence as legal-review text from a selected authority only when at least four substantive tokens have a conservative 0.8-or-higher match to a verified exact authority sentence. It remains review-required and non-filing. The sentence-map API continues to discard client-supplied citation, span, freshness, and text and re-resolve the selected source on the canonical API; a mismatched source hash fails closed with `409 sentence_support_authority_hash_mismatch`. Focused regression: **17 passed, 0 failed, 0 errors, 0 skipped** (`08_v8_drafting_authority_boundary_regression_r10.xml`).

- **Frozen r9 exact-authority drafting evidence (2026-08-29):** the exact unsigned offline `8.0.0.0` candidate `e961dd40…a7f5b8` passed private-data, bundled-engine, sealed-payload, sealed-archive, path, and size-budget audits. Against a temporary fictional matter and the separately stored active authority build, it passed both citation/quote and sentence-support journeys. The sentence-support journey selected one admitted `19-A M.R.S. § 1653(1)` pinpoint, rejected a forged client hash, re-resolved authority locally, exposed the selected source card, kept the encrypted map review-required/non-filing, denied cross-matter retrieval, and observed zero external connections. The citation/quote journey likewise required explicit pinpoint selection, preserved originals, rejected an invalid authority ID, verified its audit chain, and observed zero external connections. Evidence: `08_v8_exact_authority_sentence_support_e2e_r9.json` and `08_v8_source_bound_drafting_e2e_r9_current_authority.json`. This proves only these bounded frozen canonical-API review workflows. It does not determine current law, prove native UI interaction or isolated Store installation, admit the FAST INTERCHANGE models, or establish Store/Enterprise/attorney/pilot certification. The MSIX is unsigned and WACK was not run.

- **Frozen r9 authority-operations checkpoint (2026-08-29):** the same exact candidate passed a read-only authority review run against the separately stored active build. It verified health; active-build and source inventories; source detail, exact-span, lineage, and citation-graph drill-down; gap, freshness, availability, parser-regression, rule-history, update-report, and build-diff review responses; and both activation and rollback acknowledgement gates. No source was refreshed, activated, rolled back, or otherwise changed; 428 process-tree samples observed zero external connections. Focused slice 31–40 contracts passed **49 tests, 0 failures, 0 errors, 0 skipped**. Evidence: `08_v8_authority_operations_e2e_r9_rerun1.json` and `08_v8_authority_operations_focused_r9.xml`. This is bounded frozen canonical-API reachability for review operations, not proof of a real staged update/activation/rollback ceremony, form synchronization, Law Court enrichment, native UI interaction, isolated Store installation, current law, or release readiness.

- **Frozen r9 authority-lifecycle checkpoint (2026-08-29):** a new disposable external-fictional-authority fixture produced two distinct, independently verified immutable builds, leaving the first active and the second staged. Through the exact frozen candidate's canonical API it reviewed the staged hash diff, refused activation without an explicit acknowledgement, activated the staged build, verified the selection after restart, explicitly rolled back to the original build, and verified the rollback after a second restart. Two durable receipts recorded the activation and rollback, and 315 process-tree samples observed zero external connections. Evidence: `08_v8_authority_activation_rollback_e2e_r9.json`; fixture contract regression: `08_authority_lifecycle_fixture_regression.xml` (**19 passed, 0 failures, 0 errors, 1 expected skip**). It proves a fictional, local selection ceremony only—never a live source update, legal currentness/effect, native UI interaction, isolated Store installation, or release readiness.

- **Frozen r9 authority browser checkpoint (2026-08-29):** a browser-driven session against the exact frozen candidate reached the canonical route through **Full Workbench → Evidence & tools → Setup**. With a disposable fictional fixture it loaded two verified builds, displayed a review-required one-source hash diff, refused activation until the acknowledgement box was selected, visibly activated the staged build, and visibly restored the original build. Local-only/review-required remained visible and the browser recorded zero console errors. Evidence: `08_v8_authority_ui_browser_e2e_r9.json`. The session also found that the similarly named Add-on Studio command opened a separate candidate-manifest shell. Source and mirrored UI assets now rename it **Authority-source candidate review (add-on)** and direct users to the canonical location; that label repair passed the focused UI/lifecycle regression (**29 tests, 0 failures, 0 errors, 2 expected skips**) but is not yet in r9 and therefore needs a new candidate build and repeat browser proof. This remains fictional local workflow evidence—not a live update, legal-currentness/effect determination, isolated Store installation, WACK, or release readiness.

- **Frozen r10 authority browser checkpoint (2026-08-29):** the new unsigned offline `8.0.0.0` candidate `a9def96c…df66d4e3` rebuilt the repaired add-on label and passed a browser-driven fixture journey. From the collapsed Chat default, the user opened **Evidence & tools** through the command palette and the app switched into the visible Workbench/Evidence destination. It showed the default **Both** search mode and enabled Child Impact Lens; reached **Setup**; displayed two fictional verified immutable builds and their review-required one-source hash difference; refused activation before acknowledgement; activated the staged build after acknowledgement; and visibly rolled the original build back after a second acknowledgement. The browser console was empty; the fixture host recorded zero external connections across 10,847 samples. A paired r10 canonical lifecycle run separately proved activation and rollback persistence across restarts with zero external connections across 316 samples and two receipts. Evidence: `08_v8_authority_ui_browser_e2e_r10.json` and `08_v8_authority_activation_rollback_e2e_r10.json`. This proves a bounded fictional local workflow only—not live source update, legal currentness/effect, isolated Store installation, WACK, model admission, or release readiness.

- **Source-only command navigation polish (2026-08-29):** browser testing revealed that the literal `&` in **Open Evidence & tools** prevented the punctuation-free query `open evidence tools` from matching. The shared production command component now normalizes punctuation/whitespace before search, and every Chat-originating source/matter command target promotes into a visible Workbench panel rather than changing hidden drawer state. Focused regression: **31 tests, 0 failures, 0 errors, 1 expected skip** (`08_authority_ui_navigation_regression_r11.xml`). This repair is not in r10 and requires a new candidate plus browser rerun before it becomes frozen-package evidence.

- **Frozen r11 command-navigation and authority-browser checkpoint (2026-08-29):** the new unsigned offline `8.0.0.0` candidate `ebda56a7…2ad73260` includes the punctuation-normalized search component and visible-navigation helper in both packaged UI mirrors. Its frozen browser proof starts in Chat with **Both** selected and Child Impact Lens enabled. The punctuation-free query `open evidence tools` returns exactly one visible **Open Evidence & tools** command and leads to visible Workbench/Evidence controls. The same session opened Setup, reviewed a fictional one-source hash diff, refused unacknowledged activation, activated the staged fictional build after acknowledgement, and visibly rolled the original build back. Browser console: zero messages; fixture host: zero external connections across 10,551 samples. A paired exact-r11 canonical lifecycle run proved activation and rollback persistence across restart with two receipts and zero external connections across 344 samples. Package engine, private-data, sealed-payload, sealed-archive, and size audits all passed. Evidence: `08_v8_authority_ui_browser_e2e_r11.json` and `08_v8_authority_activation_rollback_e2e_r11.json`. This remains bounded fictional local workflow evidence—not live authority update, legal currentness/effect, isolated Store installation, WACK, FAST INTERCHANGE admission, attorney review, or release readiness.

- **Next direct evidence target:** resume the highest-priority explicitly unfinished checklist item in the 31–40 authority-update/source-lineage track. Preserve the completed local lifecycle boundary; do not mutate the active real authority root or represent a fictional fixture as a live update.

## Twenty major capability tracks

These are the twenty substantial additions represented by the 200 slices; each is decomposed below so it can be tested and released honestly rather than shipped as a broad, untestable promise.

1. Streaming, cancellable, source-safe AI chat (1–10)
2. Calibrated conversation control and correction (11–20)
3. Hybrid official-authority retrieval and citation graph (21–30)
4. Atomic authority updates, freshness, and source lineage (31–40)
5. Matter fact graph, issue-to-proof mapping, and completeness review (41–50)
6. Resumable document intelligence and scanner ingestion (51–60)
7. Audio, video, screenshot, and chain-of-custody evidence workbench (61–70)
8. Source-bound drafting, revision, citations, and exports (71–80)
9. Guided procedure, deadlines, service, forms, and filing readiness (81–90)
10. Parenting, finance, settlement, and compliance planning workspaces (91–100)
11. Hardware-aware local AI runtime and admitted-model management (101–110)
12. Universal navigation, search, saved views, and work continuity (111–120)
13. Modular accessible UI, keyboard control, and visual regression coverage (121–130)
14. Per-matter cryptography, parser isolation, and adversarial defenses (131–140)
15. Durable jobs, recovery, storage resilience, and performance observability (141–150)
16. Encrypted backups, migrations, Store-install automation, and release controls (151–160)
17. Enterprise governance, policies, legal holds, and audit verification (161–170)
18. Reviewer collaboration, exports, printing, and interoperability receipts (171–180)
19. Attorney-quality evaluation, accessibility review, and controlled pilot operations (181–190)
20. Jurisdiction packs, extension sandboxing, reproducible releases, and Enterprise-GA evidence (191–200)

## Universal definition of done

Every product slice must complete the real production path:

`service -> canonical loopback API -> matter/tenant/role enforcement -> encrypted private state -> append-only audit -> shipped desktop UI -> meaningful fictional action -> exact source/artifact drill-down -> visible review-required status -> focused and adversarial tests -> frozen-runtime reachability -> MSIX asset/privacy verification`

Additional rules:

- Never infer a legal conclusion, fact, filing status, tribal status, jurisdiction, credibility, or safety outcome.
- Keep official authority, private evidence, and model analysis visibly separate.
- Fail closed for missing, stale, wrong-jurisdiction, contradicted, or unverifiable support.
- Measure cold start, first useful feedback, first token or first result, total duration, memory, cancellation, and restart behavior where relevant.
- Do not advertise a slice until production-UI and frozen-runtime evidence pass.

## Wave 1 - Chat speed and immediate usefulness

1. **Streaming answer protocol** - Stream structured answer sections, citations, and verifier states over the canonical API; cancellation and interrupted-stream recovery must be deterministic.
2. **First-useful-feedback budget** - Show honest retrieval/rerank/verification progress within 150 ms and enforce performance budgets in CI.
3. **Fast answer path** - Route simple navigation, record lookup, definition, and status questions without invoking the full research pipeline.
4. **Progressive grounded answers** - Render a concise sourced answer first, then expand analysis, missing information, and next actions without changing the cited basis.
5. **Conversation context compaction** - Summarize long chats into source-bound, matter-scoped memory with reversible inspection and no silent fact promotion.
6. **Answer intent router v2** - Distinguish explain, locate, compare, draft, review, calculate, prepare, and navigate intents with calibrated ambiguity handling.
7. **Actionable answer footer** - End each response with the safest next action, required inputs, blockers, and one-click opening of the relevant workspace.
8. **Inline source hover and preview** - Open exact spans, freshness, jurisdiction, and source class without leaving the conversation.
9. **Answer correction workflow** - Let users flag a sentence, attach a reason, rerun verification, and preserve the original and corrected versions immutably.
10. **Chat latency observatory** - Record local-only stage timings, queue delay, model tokens, cache hits, and hardware context without private prompt text.

## Wave 2 - Answer quality and conversational control

11. **Response depth control** - Add concise, standard, and thorough modes while preserving the same verifier and safety requirements.
12. **Audience-aware explanations** - Support self-represented litigant, legal-aid intake, paralegal, and attorney review modes without changing legal truth.
13. **Clarification minimizer** - Ask only questions that materially change retrieval, procedure, drafting, or safety; explain why each is needed.
14. **Assumption ledger** - Display facts assumed, disputed, unknown, or inferred and allow the user to correct each one.
15. **Answer comparison** - Compare two generated approaches against the same sources, evidence, omissions, and risk flags.
16. **Conversation branching** - Fork a chat from any message while preserving source lineage and matter isolation.
17. **Pinned conversation facts** - Pin verified matter facts with source locators, effective dates, and dispute status for reuse.
18. **Question decomposition** - Break compound questions into independently sourced subquestions and report unresolved parts explicitly.
19. **Contradiction-aware follow-ups** - Detect when a new user statement conflicts with an order, record, or prior verified statement and request review.
20. **Response usefulness evaluation** - Add deterministic and human-review rubrics for correctness, actionability, clarity, restraint, and citation sufficiency.

## Wave 3 - Retrieval and official-authority excellence

21. **Hybrid retrieval v2** - Fuse lexical, vector, citation, heading, and metadata retrieval with explainable rank contributions.
22. **Query expansion with guardrails** - Generate Maine-law synonyms and procedural variants without changing jurisdiction or issue scope.
23. **Cross-encoder reranking** - Add an admitted local reranker with hardware-aware fallback and reproducible evaluation.
24. **Citation graph retrieval** - Traverse statutes, rules, opinions, forms, amendments, and cited authorities while preserving source hierarchy.
25. **Temporal authority resolver** - Answer “law as of date” questions using effective dates, amendments, supersession, and freshness blockers.
26. **Negative-treatment review lane** - Surface available subsequent-treatment signals without claiming comprehensive citator status.
27. **Authority conflict explainer** - Compare apparently conflicting sources by class, date, court, scope, and exact language.
28. **Pinpoint resolver expansion** - Support nested statute paragraphs, rule subdivisions, slip-opinion paragraphs, page spans, and form revisions.
29. **Retrieval diversity control** - Prevent redundant source cards and require coverage across the material source classes for the question.
30. **Authority gap detector** - Identify issue areas where the active admitted corpus lacks current or complete official sources.

## Wave 4 - Authority operations and freshness

31. **Atomic authority update center** - Download only by explicit operator action, validate, stage, diff, activate, and roll back accepted builds.
32. **Source-change impact analysis** - Map an amended source to affected saved research, drafts, forms, deadlines, and packets.
33. **Freshness service-level dashboard** - Show source-class freshness thresholds, overdue sources, parser failures, and last accepted build.
34. **Official-source availability monitor** - Detect moved URLs, changed hashes, TLS failures, and access restrictions without silent mirror substitution.
35. **Parser regression corpus** - Maintain versioned official-page fixtures covering layout changes, footnotes, tables, and malformed downloads.
36. **Authority lineage inspector** - Drill from answer span to parsed node, snapshot, retrieval event, official URL, and build fingerprint.
37. **Forms catalog synchronizer** - Compare installed form metadata with current official catalog revisions and block stale completion paths.
38. **Law Court opinion enrichment** - Add disposition, date, docket, panel, paragraph map, cited authorities, and neutral case summary with exact spans.
39. **Rule-history timeline** - Show amendment history and effective dates for Maine procedural and evidentiary rules.
40. **Offline authority portability** - Export and import signed, hash-verified authority bundles without packaging authority data in the MSIX.

## Wave 5 - Matter intelligence foundation

41. **Timeline correction acceptance** - Complete event correction, append-only history, source rebinding, UI drill-down, and frozen-app proof.
42. **Claim-disposition acceptance** - Complete support, contradiction, qualification, missing-context, and reviewer-decision workflows.
43. **Whole-matter command center acceptance** - Finish snapshots, blocker aggregation, health history, and exact corrective actions.
44. **Missing-attachment coverage acceptance** - Distinguish alleged, referenced, expected, absent, and not-yet-reviewed attachments.
45. **Matter fact graph** - Link people, events, orders, assertions, records, and sources while preserving disputed/unknown states.
46. **Issue-to-proof matrix** - Map each issue to legal elements, supporting evidence, contradictions, missing proof, and authority.
47. **Matter change digest** - Summarize new records, altered conclusions, new contradictions, deadlines, and stale work since last review.
48. **Record supersession graph** - Track originals, duplicates, changed copies, corrected OCR, translations, redactions, and exported derivatives.
49. **Cross-document entity resolution** - Resolve likely duplicate entities with explicit reviewer confirmation and reversible merges.
50. **Matter completeness scoring** - Produce explainable coverage dimensions and blockers without predicting case outcomes.

## Wave 6 - Document ingestion and intelligence

51. **Incremental large-matter ingest** - Resume interrupted imports, deduplicate before parsing, and keep the UI responsive.
52. **Watch-folder review queue** - Offer an opt-in local candidate queue without silently importing or running as a hidden service.
53. **Scanner ingestion workflow** - Add scan profiles, duplex cleanup, blank-page review, orientation, and immutable originals.
54. **Handwriting review lane** - Identify probable handwriting and route uncertain OCR to human transcription without overstating confidence.
55. **Document type classifier v2** - Classify pleadings, orders, affidavits, correspondence, financial records, forms, and exhibits with review.
56. **Page-level quality map** - Show OCR confidence, skew, blur, missing text, parser fallbacks, and pages requiring review.
57. **Table lineage inspector** - Bind every extracted cell to page coordinates, OCR text, parser output, and reviewer corrections.
58. **Document comparison v2** - Compare text, structure, tables, signatures, metadata, and page images across versions.
59. **Batch metadata editor** - Apply reviewed labels, dates, custodians, confidentiality, and document types with immutable audit history.
60. **Import policy profiles** - Save matter-specific size, format, privacy, OCR, and quarantine rules without weakening global safeguards.

## Wave 7 - Evidence and multimedia

61. **Audio transcript correction studio** - Edit timestamped segments with immutable original transcript, confidence, and reviewer history.
62. **Speaker labeling review** - Suggest speaker clusters locally and require explicit naming/confirmation before reuse.
63. **Video keyframe evidence review** - Generate local keyframes, timestamps, visual hashes, and source-bound annotations.
64. **Media redaction derivatives** - Create review-required audio muting and video blur proposals while preserving originals.
65. **Screenshot conversation reconstruction** - Order message screenshots using visible timestamps and metadata, exposing gaps and uncertainty.
66. **EXIF and media metadata inspector** - Present available metadata, absence, conflicts, and derivative effects without authentication claims.
67. **Evidence relationship graph v2** - Add temporal, attachment, reply, duplicate, contradiction, and derivative edges with exact provenance.
68. **Exhibit admission checklist** - Completed: encrypted, matter-scoped, source-hash-bound reviewer prompts organize foundation questions, authenticity materials, objection candidates, and missing-proof follow-up without legal conclusions; the desktop UI exposes creation and exact-source reference drill-down, with focused API/service coverage.
69. **Chain-of-custody event capture** - Completed: encrypted, matter-scoped collection, transfer, transformation, hashing, review, and export observations are source-hash-bound, user-confirmed, hash-linked, and protected by local matter-key integrity receipts; the desktop Exhibit workspace can capture, verify, and drill into each source without treating the receipt as an authenticity or legal signature.
70. **Courtroom media player** - Completed: the local hearing-media workbench creates source-hash-bound, confirmed audio/video clip-review sessions; bounded offline embedded playback offers keyboard controls and timestamped transcript sync, while review-confirmed private notes live in a separate encrypted store and are excluded from the session record and export path.

## Wave 8 - Drafting and revision excellence

71. **Tracked-DOCX installed workflow acceptance** - Source-checkout acceptance completed: a fictional DOCX import, hash-anchored paragraph lookup, explicitly confirmed tracked replacement and comment, new-copy download, reimport, comparison, and audit validation are covered; Store build inputs explicitly include docx-editor, python-docx, and the package-relative comment templates required by the frozen executable. The rebuilt package then passed the direct frozen-runtime canonical-API path with a session-bound artifact replay denial, source hash, receipt hash, review-required/non-filing state, audit-chain, and zero-observed-external-connection checks. Native UI interaction, isolated MSIX installation, and Store qualification remain separately unproven.
72. **Structured draft outline builder** - Encrypted, matter-scoped outlines require explicit reviewer confirmation plus selected hash-bound private evidence and selected hash-bound authority. The production Document workspace can create an outline, inspect its exact private record, and open its official source while keeping the lanes separate and review-required. The current frozen package now proves the canonical-API path with a separately stored official authority root, private and authority source drill-down, encryption at rest, cross-matter denial, and zero observed external TCP connections. It records the authority source's stored freshness without asserting current law. Browser/native UI interaction, isolated MSIX installation, and Store qualification remain separately unproven.
73. **Sentence-level support map** - An encrypted, revision-bound map evaluates every saved-draft sentence against hash-bound active-matter record text and selected authority spans. It exposes separate support, contradiction, qualification, and missing-context signals with exact-source actions, detects stale maps after a draft revision, and never treats the result as a factual or legal conclusion. The frozen package now proves factual support, private-source drill-down, encrypted state, cross-matter denial, and fail-closed missing-context behavior for a selected authority without an exact span; the external authority root remains outside the MSIX. Browser/native UI interaction, isolated MSIX installation, and Store qualification remain separately unproven.
74. **Citation insertion assistant** - Implemented and hardened in the source checkout: encrypted insertion receipts require reviewer confirmation, an exact current-draft selection, and a source-provided pinpoint. The canonical API now replaces every client-supplied authority field with a locally resolved source card and rejects a supplied-hash mismatch, so a direct caller cannot invent a citation, pinpoint, span, or freshness status. Stale authority and missing source-provided pinpoints fail closed. Against the current external immutable authority build, the exact frozen v8 candidate proved that multiple statute pinpoints require a reviewer choice, an invalid authority ID is rejected, the proposed revision preserves the original, cross-matter receipt access is denied, audit verification passes, and no external connections were observed. The shipped UI assets expose explicit citation/quote span selectors; browser/native interaction, isolated MSIX installation, and Store qualification remain unproven.
75. **Quote-safe drafting** - Implemented and hardened in the source checkout: an encrypted quote receipt replaces exact selected draft text only when the proposed quote is found exactly in a locally resolved official source span, or when a reviewer separately approves a normalization-only match. The canonical API now replaces client-supplied authority fields with the local resolver result and rejects a supplied-hash mismatch. The same frozen v8 candidate proved an exact review-required/non-filing quote receipt, original-preserving proposal, cross-matter isolation, audit integrity, and zero observed external connections using a reviewer-selected official source span. Fuzzy, stale, unconfirmed, duplicate-selection, and stale-revision paths fail closed. Browser/native interaction, isolated MSIX installation, and Store qualification remain unproven.
76. **Draft requirement profiles** - Completed in the source checkout: encrypted, matter-scoped local profiles require explicit confirmation and evaluate only reviewer-specified sections, character limits, and review gates against a saved draft. UI and API make the non-court-approved, review-required boundary explicit; no evaluation can mark a document filing ready. The exact frozen v8 candidate now proves a fictional active-matter create → evaluate → list path, encrypted-at-rest state, cross-matter profile/document denial, review-required/non-filing state, and zero observed external connections. Browser/native UI interaction, isolated MSIX installation, and Store qualification remain separately unproven.
77. **Revision rationale ledger** - Completed in the source checkout: encrypted, matter-scoped rationale records bind reviewer-safe ID, change summary, reason, affected claim IDs, and verifier-impact state to the exact saved document revision. The drafting UI can record and load the history while retaining review-required status. The exact frozen v8 candidate now proves fictional create → load, revision binding, encrypted-at-rest ledger state, cross-matter denial, review-required/non-filing status, and zero observed external connections. Browser/native UI interaction, isolated MSIX installation, and Store qualification remain separately unproven.
78. **Plain-language dual view** - Completed in the source checkout: encrypted, matter-scoped legal-review and plain-language working copies bind to the exact saved revision and normalized source-reference set, fail closed without a reviewer confirmation, expose stale-revision status, retain an audit chain, and never become filing-ready. The production Document workspace can create, reload, and drill into linked sources. The exact frozen v8 candidate now proves a fictional canonical-authority detail lookup, source-hash/reference retention, create → reload on the current revision, review-required/non-filing state, encrypted-at-rest view ledger, cross-matter denial, and zero observed external connections. Browser/native interaction, isolated MSIX installation, and Store qualification remain separately unproven.
79. **Argument/counterargument matrix** - Completed in the source checkout: encrypted, matter-scoped reviewer matrices require explicit confirmation, bind each competing position to active-matter record hashes and resolver-revalidated official authority, and retain weaknesses and missing-proof prompts without predicting outcomes. The production Document workspace can create, reload, and inspect private-record or official-authority source lanes while all matrices remain review-required and not filing-ready. The production UI now requires an explicit precise-source-span choice when a source provides multiple candidates; it never chooses legal language for the reviewer. The exact frozen v8 candidate proves a fictional two-position creation → reload flow, separate source lanes, explicit precise authority selection, private and official source drill-down, encrypted-at-rest state, cross-matter denial, review-required/non-predictive/non-filing state, and zero observed external connections. Browser/native interaction, isolated MSIX installation, and Store qualification remain separately unproven.
80. **Export provenance footer** - Completed in the source checkout: TXT, Markdown, and DOCX document exports receive a server-generated footer bound to the exact document revision, content hash, normalized source snapshot, product version, opaque matter-scope identifier, review state, local privacy state, and a matter-private receipt ID. Artifact hashes finalize the encrypted receipt; public API/UI status exposes no local paths and every export remains review-required and not filing-ready. The exact frozen v8 candidate now proves a fictional TXT export-session → local download → completed-receipt flow with the server-generated review footer, canonical authority source detail, exact revision/source-snapshot receipt binding, encrypted-at-rest receipt state, raw-path exclusion, cross-matter denial, and zero observed external connections. Browser/native interaction, isolated MSIX installation, and Store qualification remain separately unproven.

## Wave 9 - Forms, procedure, and deadlines

81. **Guided forms acceptance** - Completed in the source checkout: the guided-form flow uses the active official catalog, keeps stale forms blocked, binds review/completion to the exact document revision, and persists new user-entered working-copy sessions in encrypted, matter-scoped, audit-linked storage. The production Document workspace can select verified current forms, open the exact official source, start or resume a session, save values, validate required fields, and inspect a receipt; legacy plain sessions migrate to the encrypted store when reopened without deleting the original. The exact frozen v8 candidate safely exercised the factual session path with an active official-form source detail, encrypted local state, review-required/non-filing validation, receipt retrieval, cross-matter denial, and zero observed external connections. However, the active external catalog supplied zero forms marked current/fresh/verified-current, so the public UI correctly exposes no selectable form and the end-to-end usable-form path is **BLOCKED** as `verified_current_authority_form_unavailable`. Do not relabel index-retrieved or unknown forms as current; direct official form snapshots with version/freshness metadata must be admitted before this feature can receive full frozen-runtime acceptance. Browser/native interaction, isolated MSIX installation, and Store qualification remain separately unproven.
82. **Procedure pathway engine v2** - Completed in the source checkout: encrypted, matter-scoped procedure pathways require reviewer confirmation, capture user-entered case type/posture/venue context, validate selected known-order hashes against the active matter, and resolver-revalidate one selected official source. The production Document workspace creates/reloads a review-only checklist and opens the exact order or official source. It does not decide venue, jurisdiction, service, deadlines, requirements, legal effect, or an outcome. The exact frozen v8 candidate now proves a fictional create → reload path with a hash-bound active-matter record, canonical official-source detail, both private and official drill-downs, encrypted-at-rest state, cross-matter denial, review-required/non-filing/non-advisory status, and zero observed external connections. Browser/native interaction, isolated MSIX installation, and Store qualification remain separately unproven.
83. **Deadline dependency graph** - Completed in the source checkout: encrypted, matter-scoped dependency calculations require explicit reviewer confirmation and a hash-bound active-matter trigger. A changed trigger creates a new review-required candidate while retaining, deactivating, and linking the prior candidate rather than silently replacing it. The production Document workspace can select the trigger record, create or reload lineage, and reopen the exact trigger through a short-lived active-matter capability. The rebuilt frozen r5 candidate proves a fictional canonical create → reload → exact-trigger-source flow, including normalized active-record identifier matching, encrypted calendar state, cross-matter denial, review-required/non-filing status, and zero observed external connections. It does not decide an actual court deadline, resolve authority freshness, or make anything filing-ready. Browser/native interaction, isolated MSIX installation, and Store qualification remain separately unproven.
84. **Service-method rules matrix** - Completed in the source checkout: an encrypted, matter-scoped reviewer matrix binds one selected method to a hash-validated private proof record and resolver-revalidated official authority while preserving possible exceptions and unresolved facts. The production Document workspace can choose a proof record, check/open the authority, create/reload the matrix, and reopen the exact proof record. Every matrix is review-required and non-filing-ready; it does not determine effective service, notice, waiver, timeliness, jurisdiction, or legal sufficiency. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
85. **Court holiday and business-day engine** - Completed in the source checkout: encrypted, matter-scoped calendar-input versions preserve reviewer-entered jurisdiction label, validity window, holiday list, official-source card, hash, and audit ledger. Candidate calculations bind to one exact input hash, skip weekends and listed dates, preserve skipped-date receipts, and fail closed outside the selected input’s validity window. The production Document workspace can check/open the official source, save a versioned input, calculate/reload a candidate, and see its review boundary. It does not establish a court holiday, deadline, jurisdiction, or legal effect. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
86. **Hearing preparation countdown** - Completed in the source checkout: encrypted, matter-scoped review countdowns require explicit confirmation, one reviewer-confirmed date, and a hash-validated active-matter notice record. They generate local-only milestone prompts and preserve reviewer-entered missing-proof prompts with audit lineage; the production Document workspace can create/reload the countdown and reopen the exact notice record. It does not confirm a hearing, notify a court or person, create a court reminder, determine a deadline, or make anything filing-ready. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
87. **Filing package preflight v2** - Completed in the source checkout: an encrypted, matter-scoped blocker preflight binds listed attachments to active-matter hashes, re-resolves selected official form/source cards, records reviewer confirmations for caption, names, signatures, format, redactions, privacy, and human review, and preserves a hash-linked audit receipt. The production Document workspace creates/reloads the blocker report and can reopen an attachment or form/source. It is permanently review-required and non-filing-ready; it adds a blocker unless a separately verified canonical reviewed-filing packet is observed, and it cannot submit, approve, or bypass that packet gate. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
88. **Fee and waiver information workspace** - Completed in the source checkout: encrypted, matter-scoped information workspaces require reviewer confirmation, resolver-revalidate one selected official fee/waiver source, and preserve user-entered facts as explicitly unverified with an audit receipt. The production Document workspace can check/open the source and create/reload a local information workspace. It does not calculate fees, decide waiver eligibility, complete a form, or make a filing ready. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
89. **Venue and court-location navigator** - Completed in the source checkout: encrypted, matter-scoped location workspaces require reviewer confirmation, resolver-revalidate one official source, preserve reviewer-entered location/contact notes and unresolved facts, and retain an audit receipt. The production Document workspace can check/open the official source and create/reload the navigator. It does not decide venue, jurisdiction, court assignment, filing location, or legal effect. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
90. **Post-filing receipt reconciliation** - Completed in the source checkout: encrypted, matter-scoped reconciliations require reviewer confirmation, a hash-validated receipt record, and hash-validated submitted records from the active matter. They compare filenames and hashes to reviewer-entered docket expectations, preserve exact/partial/unmatched decisions with audit lineage, and let the production Document workspace reopen the receipt or submitted record. It does not confirm court acceptance, docket entry, submission, or filing readiness. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.

- **Frozen runtime / MSIX status for slices 84–90 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed fourteen checks in one disposable fictional matter with an explicitly configured external authority root. It exercised the service-method matrix, business-day review candidate, hearing countdown, filing-preflight blocker, fee/waiver workspace, venue/location workspace, and post-filing reconciliation through the frozen canonical API. The run verified shipped production-workbench entry strings/routes, encrypted review sidecars, private source drill-down, cross-matter denial for all seven stores, visible review-required/non-filing or non-determinative states, and zero observed external TCP connections across 383 samples. The authority root remained outside the MSIX. This does not prove browser/native interaction, actual court procedure, deadline/service/venue/fee eligibility, current law, isolated installation, Store qualification, or Enterprise GA. Evidence: `dist/ga_today/evidence/08_v8_procedure_review_e2e_r6_rerun2.json`.

## Wave 10 - Parenting, finance, and resolution support

91. **Parenting schedule simulation v2** - Completed in the source checkout: encrypted, matter-scoped neutral simulations require reviewer confirmation and hash-validate every event against an active-matter record. They preserve reviewer-entered parenting-time, travel, holiday, school, and exchange events, flag date overlaps for review, retain audit lineage, and never recommend a schedule or custody outcome. The production Document workspace creates/reloads a comparison and reopens its exact private source record. It does not determine legal effect, write a calendar, decide custody, or make anything filing-ready. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
92. **Order-to-calendar extraction** - Completed in the source checkout: encrypted, matter-scoped local candidates require explicit reviewer confirmation and a previously reviewer-confirmed exact order term. The service revalidates the term’s source hash against the active-matter record, retains the exact language and review lineage, and produces a local review-required calendar candidate without external-calendar writes. The production Document workspace loads confirmed terms, creates/reloads a candidate, and opens the exact order record. It does not decide that an order governs, interpret a term, notify a person, or make anything filing-ready. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
93. **Child-support worksheet preparation** - Completed in the source checkout: encrypted, matter-scoped preparation workspaces require explicit reviewer confirmation and a selected current official authority card; stale or unknown authority is rejected. They preserve user-entered inputs as explicitly unverified or unknown, retain missing facts with audit lineage, and expose the exact authority source in the production Document workspace. They never calculate support, determine income, deviations, eligibility, legal effect, or worksheet completion. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
94. **Financial affidavit workbench** - Completed in the source checkout: encrypted, matter-scoped workspaces require reviewer confirmation and hash-bind every income, expense, asset, debt, or unknown row to an active-matter record. They retain reviewer-entered values, expose differing values for the same reconciliation key, preserve unknowns and audit lineage, and let the production Document workspace reopen each exact record. They do not calculate totals, determine income/value/debt, reconcile a fact, create an affidavit, or make a sworn or filing-ready conclusion. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
95. **Asset tracing ledger** - Completed in the source checkout: encrypted, matter-scoped ledgers require reviewer confirmation and hash-bind each claimed asset to active-matter supporting records. They retain claimed source, transfer notes, valuation-date references, characterization assertions, and a visible dispute state with audit lineage; the production Document workspace creates/reloads a ledger and opens its exact supporting record. They do not determine ownership, marital/nonmarital characterization, value, division, or legal effect. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
96. **Debt reconciliation workspace** - Completed in the source checkout: encrypted, matter-scoped workspaces require reviewer confirmation and hash-bind each statement to an active-matter record. They preserve reviewer-entered period, balance, responsibility, payment, and missing-period assertions, surface mismatched balances and gaps by account key, retain audit lineage, and let the production Document workspace reopen the exact statement record. They do not determine that a debt exists, a balance, responsibility, payment, validity, or legal effect. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
97. **Settlement scenario comparator** - Completed in the source checkout: encrypted, matter-scoped comparisons require explicit reviewer confirmation and hash-bind each of two user-entered scenarios to an active-matter record. They compare schedules, property, support, implementation, and unresolved terms; retain audit lineage; and let the production Document workspace reopen an exact source. They do not recommend, approve, interpret, enforce, or predict an agreement or outcome. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
98. **Implementation feasibility review** - Completed in the source checkout: encrypted, matter-scoped, hash-bound clause reviews require reviewer confirmation and flag possible internal conflicts, undefined/ambiguous terms, and missing date/timing text for review. The production Document workspace creates/reloads the review and opens its exact clause source. It does not determine validity, enforceability, legal effect, agreement, or outcome. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
99. **Communication-plan builder** - Completed in the source checkout: encrypted, matter-scoped plans require reviewer confirmation and hash-bind selected neutral terms to active-matter records. The production Document workspace creates/reloads a neutral review draft and opens its exact source. It does not determine safety, consent, agreement, enforceability, legal effect, or an outcome. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
100. **Compliance log** - Completed in the source checkout: encrypted, matter-scoped logs require reviewer confirmation, source-bind an allegation or observation to both an exact selected order term and an active-matter event record, retain audit lineage, and let the production Document workspace reopen the event source. They keep allegation, observation, and finding-not-available distinct and do not determine compliance, violation, contempt, credibility, or a finding. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.

- **Frozen runtime / MSIX status for slices 91–100 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed twelve checks in one disposable fictional matter with an explicitly configured external authority root. It exercised the parenting simulation, confirmed-order calendar candidate, financial-affidavit, asset-tracing, debt-reconciliation, settlement, feasibility, communication-plan, and compliance-log workflows through the frozen canonical API. It verified review-only/non-determinative states, encrypted sidecars, private source drill-down, cross-matter denial, shipped production-workbench entry strings/routes, and zero observed external TCP connections across 329 samples. The child-support workspace correctly rejected the selected external source as not `current` (`child_support_current_authority_required`, HTTP 409); no record was created and no authority was relabeled to bypass that gate. The authority root remained outside the MSIX. This does not prove browser/native interaction, an actual parenting/financial/compliance conclusion, current law, isolated installation, Store qualification, or Enterprise GA. Evidence: `dist/ga_today/evidence/08_v8_family_finance_e2e_r6.json`.

## Wave 11 - Local AI runtime and hardware performance

101. **Hardware benchmark wizard** - Completed in the source checkout: an encrypted local-only hardware benchmark measures CPU cores, RAM where available, storage, and a bounded CPU probe; it records conservative worker/batch recommendations without changing settings. GPU and model throughput are explicitly reported as not measured until an admitted runtime is benchmarked. The production Document workspace runs/reloads the local receipt. No model download, admission, setting change, telemetry, or network request occurs. Browser, frozen-runtime, and rebuilt-package interaction remain separately unproven.
102. **Model admission benchmark** - Implemented in the source checkout: a production UI can run a loopback-only benchmark against an operator-selected local provider/model, record latency, submitted context, structured-output parsing, and direct-prompt safety behavior in encrypted state, and keep the result permanently review-required and non-admitting. Provider memory is explicitly not measured when absent from the runtime. The runner has focused fake-runtime coverage, but no live loopback model was running during this pass; live model evidence, actual latency/memory/context results, and admission remain unproven.
103. **Task-specific model routing** - Completed in the source checkout: the existing local-workbench router now fails closed and selects only separately task-admitted local models; registered or artifact-verified models are excluded and the deterministic/existing-local-agent fallback remains selected. The production Document workspace can check a task route and surfaces its admission boundary. Focused control-plane coverage passed; no admitted local model or live model-task execution was available during this pass, so live routing performance remains unproven.
104. **Warm model pool** - Implemented in the source checkout: encrypted matter-scoped pool state can warm only an already task-admitted, loopback-configured worker with synthetic text, records a tamper-evident receipt, and explicitly releases the worker on an operator or thermal-pressure request. It refuses unadmitted models and providers without an explicit release capability. The production Document workspace exposes warm, status, and release actions with the admission boundary. Focused service/API/UI tests passed; no task-admitted model or live local model server was available, so a real warm/release cycle, frozen-runtime reachability, and package evidence remain unproven.
105. **Prompt-prefix and retrieval cache** - Implemented in the source checkout: the production UI can save bounded prompt-prefix or retrieval artifacts only with exact source IDs and hashes. All entries are encrypted and matter-scoped; the public-authority scope refuses private-record references. Hash changes or source removal invalidate dependent entries and preserve a review-required audit trail. Focused service/API/UI tests passed; the cache has not been exercised in a frozen executable or MSIX in this pass.
106. **Speculative retrieval** - Implemented in the source checkout: a debounce-driven production UI can stage a discardable local authority preview from typed intent. The intent is injection-sanitized before local retrieval, saved only in encrypted matter-scoped state, and yields compact source candidates rather than an answer. The preview declares that no answer is committed and provides candidate inspection and discard actions. Focused service/API/UI tests passed; no frozen-runtime or MSIX evidence was collected in this pass.
107. **Adaptive context budgeting** - Implemented in the source checkout: an encrypted, matter-scoped budgeting path allocates reviewable local context from task type, exact source density, current hardware limits, and explicit citation, quote, and claim-verifier reservations. It preserves source hash references, caps unsafe requests, does not run a model, and exposes an inspectable allocation in the production UI. Focused service/API/UI tests passed; frozen-runtime and package reachability remain unproven.
108. **Batch inference scheduler** - Implemented in the source checkout: a matter-scoped, encrypted scheduler groups only compatible local extract, classify, or embed jobs and creates durable kernel jobs with source IDs/hashes only. Each child is independently cancellable; a shared durable job cancels only when every child within it is cancelled. The production UI schedules, loads, drills into a child source reference, and cancels children. Scheduling is explicitly non-executing and review-required. Focused service/API/UI tests passed; no live model worker, frozen-runtime, or package proof was collected in this pass.
109. **Graceful low-memory mode** - Implemented in the source checkout: a reversible encrypted matter-local posture exposes a clear degraded state and the constrained settings—lexical retrieval, compact admitted model or deterministic fallback, single-item batches, 2048-token cap, and warm-worker release. The adaptive context budget now imposes the 2048-token cap when the hardware reports under 4 GiB available. Production UI controls enable, inspect, and disable the posture. Focused service/API/UI tests passed; live retrieval/model/batch execution, frozen-runtime, and package proof remain unproven.
110. **Runtime crash recovery** - Implemented in the source checkout: a matter-scoped recovery path inspects durable jobs, requeues only safely recoverable work abandoned by an expired lease, preserves completed/cancelled records, and retains terminal failures for human review instead of silently retrying them. The UI displays the requeued, preserved, discarded, and failed counts and can inspect job-event receipts. Focused kernel/service/API/UI tests passed; a real process crash, frozen-runtime recovery, and packaged application restart remain unproven.

- **Frozen runtime / MSIX status for slices 101–110 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed fourteen checks in one disposable fictional matter. It exercised the local hardware receipt, task-routing fallback, unadmitted-model warm-pool refusal, encrypted cache plus changed-source invalidation, discardable speculative retrieval, verifier-reserved context budget, non-executing batch scheduling plus child cancellation, reversible low-memory posture, and no-expired-worker recovery report. It verified the shipped production-workbench entry strings/routes, encrypted runtime sidecars, cross-matter denial, and zero observed external TCP connections across 816 samples. The selected route was `cpu_fallback_or_review_required`; the warm pool returned `not_warmed_no_task_admission_review_required`. No model was downloaded, admitted, invoked, or promoted. The prior r0001 Fast Interchange completion failure remains a separate model blocker. This does not prove an admitted-model benchmark, live model execution, actual hardware capacity, real crash/restart recovery, browser/native interaction, isolated installation, Store qualification, or Enterprise GA. Evidence: `dist/ga_today/evidence/08_v8_runtime_management_e2e_r6_rerun1.json`.

## Wave 12 - Search, navigation, and personal productivity

111. **Universal command bar v2** - Implemented in the source checkout: the existing keyboard-first command palette now supplements its local command inventory with canonical active-matter search results for the active matter, records, admitted-source candidates, drafting outlines, and settings. Every result carries a required permission label and a safe navigation target; record results use an opaque capability token where available and no result exposes a case-root path. Focused service/API/UI tests passed; frozen-runtime and MSIX reachability remain unproven.
112. **Unified matter search** - Implemented in the source checkout: the production UI now searches the active matter from one surface across safe record metadata, document dates, citations, annotations, privacy state, and review state. Results remain matter-scoped, carry a review-required boundary, and use an opaque record capability for exact inspection. Focused service/API/UI tests passed; frozen-runtime and MSIX reachability remain unproven.
113. **Saved smart views** - Implemented in the source checkout: encrypted matter-scoped views can be saved and run for deadlines, blockers, missing proof, unread records, and review queues. The production UI creates a confirmed local filter, runs it against the active matter, and opens an opaque-capability-backed first result. Views are explicitly review filters, not deadline calculations or findings. Focused service/API/UI tests passed; frozen-runtime and MSIX evidence remain unproven.
114. **Recent-work continuity** - Implemented in the source checkout: an encrypted active-matter restore point keeps a bounded scroll position, selected source references, and an unsent chat draft without persisting a filesystem path or reusable capability token. The production chat UI exposes restore, source reopening, and clear actions; private record reopening rechecks the active matter record ID/hash and mints a fresh short-lived capability. The restore point stays review-required and cannot alter records, drafts, or findings. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX restart proof remain unproven.
115. **Workspace tabs** - Implemented in the source checkout: encrypted active-matter tabs retain bounded record, official-source, draft, and comparison references with tamper-evident audit events and an explicit review boundary. The production Document workspace can create, list, activate, reopen, and close tabs without clearing the chat; private record tabs must match an active-matter record/hash at creation and are revalidated before a fresh opaque inspection capability is minted. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX multi-context reachability remain unproven.
116. **Command history and replay** - Implemented in the source checkout: encrypted, matter-scoped command history records a bounded allow-list of read operations and mutation intents with tamper-evident audit events. The production Document workspace can replay active-matter search, authority search, and saved-view reads and reopen a returned source; a mutation intent requires an initial and fresh confirmation, then is only marked reconfirmed—not executed. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX replay reachability remain unproven.
117. **Bulk review queue** - Implemented in the source checkout: encrypted, matter-scoped queue items cover record, claim, citation, privacy-finding, and correction review categories; each requires explicit confirmation and an exact active-matter record/hash reference. The production Document workspace can create, list, triage, keyboard-select (up/down), mark needs-review or deferred (R/D after confirmation), and reopen the exact source. Triage keeps a visible review boundary and cannot create a factual, privacy, legal, or filing conclusion. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX keyboard-flow proof remain unproven.
118. **Favorites and pinning** - Implemented in the source checkout: encrypted active-matter favorites can pin matter, record, official-source, draft, and workspace references with a bounded label, explicit confirmation, tamper-evident audit history, and a local role-filtered visibility setting. Record favorites require an active-matter ID/hash at creation and revalidate before minting an inspection capability; the production Document workspace can create, list, open, and remove pins. This desktop build has no identity-backed multi-user session, so visibility is clearly labeled as a local presentation filter—not authorization or identity proof. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX reachability remain unproven.
119. **User-defined labels** - Implemented in the source checkout: encrypted active-matter labels use NFKC/case-folded collision detection, bounded names/colors, tamper-evident creation/assignment/import events, and exact record ID/hash assignments. The production Document workspace can create labels, select an active-matter record, apply a label, create an explicit local export, and import a prior export with collision-safe renaming. Labels remain organization metadata, not legal/factual classification or filing status. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX migration reachability remain unproven.
120. **Daily matter brief** - Implemented in the source checkout: an explicitly requested, encrypted active-matter digest groups current metadata signals for changed records, due reviews, deadline candidates, and potential blockers. The production Document workspace can build and reload a named brief, with a clear review-only boundary and audit receipt. It does not decide that a record changed, a deadline applies, a matter is complete, or a blocker/legal effect exists. Focused fictional-data service/API/UI tests passed; browser, frozen-runtime, and MSIX reachability remain unproven.

- **Frozen runtime / MSIX status for slices 111–120 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed twelve checks in one disposable fictional matter. It exercised command-bar and unified-matter search, a saved review filter, encrypted recent-work restoration, record workspace tab activation, safe read-only command replay, queue creation and triage, a local-role-filtered favorite, label assignment/export, and an explicit daily review brief. It verified shipped production-workbench entry strings/routes, encrypted sidecars, fresh local record-source capabilities, cross-matter denial or isolated empty restore state after a matter switch, and zero observed external TCP connections across 303 samples. This does not prove browser/native keyboard interaction, conclusions from a saved filter or brief, user identity/authorization, isolated installation, Store qualification, or Enterprise GA. Evidence: `dist/ga_today/evidence/08_v8_navigation_continuity_e2e_r6_rerun1.json`.

## Wave 13 - UI, accessibility, and interaction quality

121. **Design-token consolidation** - Implemented in the source checkout: the production workbench now has semantic canvas, surface, text, muted-text, border, action, focus, status, spacing, type, motion, and focus-offset tokens. The v8 production layer maps its visual palette to those tokens and uses the shared focus ring while retaining reduced-motion and forced-colors overrides. Existing UX/accessibility regression coverage plus focused token/mirror tests passed. The production source UI rendered successfully in default and compact browser states; cross-theme, forced-colors, zoom, frozen-package, and MSIX proof remain unproven.
122. **Componentized production UI** - Implemented in the source checkout: the production workbench retains its stable HTML/CSS/controller entrypoints while a dependency-free, separately tested component module now owns deterministic command filtering/grouping and non-wrapping listbox navigation. It is loaded before the controller, explicitly inventoried in the production/frozen asset contract, and collected by the existing recursive PyInstaller UI-asset rule. Focused component, manifest, mirror, JavaScript syntax, and existing UX checks passed. The source production workbench rendered its chat and full-workbench views in-browser; frozen-executable and MSIX reachability remain unproven.
123. **Virtualized long lists** - Implemented a bounded, progressive source-card list in the production workbench: filtering operates against the complete in-memory result set, only an initial window of 60 matching cards enters the DOM, and an explicit keyboard-accessible “Show next” action advances the window without discarding source IDs, drill-down, export, or review metadata. This slice does not claim that every legacy workspace list, browser performance profile, frozen executable, or MSIX path has been benchmarked.
124. **Responsive workspace rules v2** - Implemented a live responsive-layout contract for the production workbench: full (≥1360px), compact desktop (≥960px), and overlay (<960px) profiles are exposed to the UI, announced accessibly, and applied on load/resize. At the overlay threshold the chat and its primary actions remain available while workspace panels remain reachable through the view menu rather than being clipped; core actions have minimum target dimensions. Existing reduced-motion and forced-colors rules remain in place. Focused source/mirror/syntax coverage passed; a rendered 960x720 production-chat run confirmed the overlay profile and visible question/send controls. Rendered 200%-zoom, high-contrast, frozen-executable, and MSIX evidence remain unproven.
125. **Screen-reader workflow audit** - Implemented a repeatable static audit of the actual packaged production assets and exposed it through the existing local UI-manifest API. The audit validates a single main landmark, skip link, live regions, dialog markup, focus-return and focusable-element handling, Escape handling, tab-panel naming, visible focus, reduced motion, forced colors, and responsive announcements; it reports dialog/live-region counts and explicit limitations. This is not represented as a screen-reader-session, frozen-executable, or MSIX proof.
126. **Keyboard command system** - Implemented discoverable, local shortcut preferences for the production command palette and Justice key. The only supported, allow-listed choices are Ctrl K or Ctrl Shift P for commands and Ctrl J or Off for the Justice key; invalid values fail closed, labels and `aria-keyshortcuts` update, and the browser-profile setting contains no matter data. Existing dialog focus-trap and Escape behavior remains in effect. Focused source/mirror/syntax checks passed; assistive-technology, frozen-executable, and MSIX evidence remain unproven.
127. **Focus and dialog manager** - Implemented a shared overlay stack for the production workbench. The topmost open overlay now owns the Tab trap and Escape path; opening a second overlay makes the prior one inert and hidden from assistive technology, while closing restores the prior overlay before focus returns. The manager preserves the original opener, leaves errors within the active dialog, and keeps existing close controls. Focused source/mirror/syntax coverage passed. The production browser run rendered both empty and populated recovery dialogs; a keyboard-only nested-dialog or screen-reader session and frozen/MSIX proof remain unproven.
128. **Density and text-size controls** - Implemented local display controls in Conversation settings. Users can choose comfortable or compact spacing and standard, large, or extra-large text; choices are allow-listed, visibly confirmed, apply through production CSS, and persist only as browser-profile presentation settings. They do not change matter data, sources, legal analysis, review state, or export content. Focused source/mirror/syntax checks passed; a browser run changed and confirmed compact/large local display preferences. Assistive-technology, frozen-executable, and MSIX evidence remain unproven.
129. **User-facing error center** - Implemented a session-only, production error center reachable from the view menu. Recoverable UI failures now record bounded safe codes, affected scope, preserved state, recovery guidance, and timestamp—without raw server responses, local paths, record text, or matter identifiers. Users can clear the session list and return to the original control to retry after correcting prerequisites. Existing error cards retain technical-code disclosure. Focused source/mirror/syntax checks passed. A controlled local-service disconnect rendered a `local_service_unreachable` entry with preserved-state/recovery guidance and no raw path or fetch-error leak; frozen-executable and MSIX evidence remain unproven.
130. **Visual regression matrix** - Implemented a production-browser visual matrix using only fictional/no-private-data interactions. It captured default chat, full workbench, 960x720 compact chat with visible question/send controls, source drill-down with freshness and review state, review-blocker workspace, empty recovery, and a controlled local-service-disconnect recovery state. Seven PNGs were validated as true PNGs and hashes were recorded in `dist/ga_today/evidence/pass130_visual_regression_matrix.json`. The source workbench/API path is proven only for the listed states. Dark/light theme changes, forced-colors/high-contrast rendering, 200% zoom, frozen executable, and MSIX rendering remain unproven rather than inferred.

- **Frozen runtime / MSIX status for slices 121–130 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed the r6 static production-asset audit. The frozen executable started and served the workbench, component module, stylesheet, controller, and runtime UI manifest; the shipped assets contained the semantic design-token, reduced-motion, forced-colors, responsive viewport, bounded source-card window, keyboard shortcut, overlay focus-manager, display-preference, and safe error-center contracts. Its embedded static accessibility audit passed with one main landmark, 40 dialogs, 37 modal dialogs, and 90 live regions. No external TCP connection was observed in 103 runtime samples. This evidence does **not** prove screen-reader, keyboard/pointer, 200% zoom, forced-colors visual, performance, isolated-installation, Store, or Enterprise testing. Evidence: `dist/ga_today/evidence/08_v8_frozen_ui_accessibility_audit_r6_rerun1.json`.

## Wave 14 - Privacy and security hardening

131. **Per-matter key hierarchy** - Implemented in the source checkout: new external matter metadata and document envelopes use independent tenant/matter AES-256-GCM data keys wrapped from local vault-derived root material; legacy envelopes remain readable without destructive migration. The hierarchy supports explicit recovery enrollment, rotation with recovery rewrap, root-wrap recovery, revocation, and confirmation-gated cryptographic deletion, with tamper-evident non-secret event state. Canonical protected API routes enforce loopback, role/tenant/matter scope, admin-only mutation, short-lived action capability, CSRF, approval, audit, and review-required responses. The shipped Privacy view can inspect only active-matter non-secret status and keeps destructive administration out of normal chat. Focused fictional-data hierarchy, service, canonical API, source/mirror asset, UI-manifest, production-gateway, and browser-smoke coverage passed. The browser smoke confirmed safe blocked status when no accessible matter was selected; an unlocked fictional-matter browser path, frozen executable, and MSIX reachability remain unproven.
132. **Windows Hello matter unlock** - Implemented in the source checkout: an optional encrypted per-matter user-presence policy uses the Windows Runtime UserConsentVerifier only when the local Hello projection reports availability. The app never captures, stores, or infers biometric data; it receives only a verified/not-verified result. Successful approval creates a short-lived, process-local, non-exportable tenant/matter grant that gates encrypted matter reads and writes; expiration/lock fails closed. Policy configuration is admin/session/CSRF/approval guarded, verification and lock require action-scoped sessions, and each status/configuration/verification is audit-recorded and review-required. The shipped Privacy view can inspect policy, explicitly request an enabled local unlock, and lock the local session; it cannot enable policy or expose recovery data. Focused fictional presence-provider/service/API/UI and browser-smoke coverage passed. This host’s browser smoke reported Windows Hello unavailable/not configured and safely retained a blocked review-required state. Native Windows Hello prompt verification, frozen-runtime, and MSIX reachability remain unproven.
133. **Sensitive clipboard controls** - Implemented in the source checkout: every production workbench clipboard write now flows through one helper. Potentially private answers, reviewer handoffs, private record details, OCR spans, and private source cards require an explicit warning/confirmation; the helper never reads, logs, hashes, previews, or stores clipboard contents. App-originated sensitive content is scheduled for a 90-second clear only while the active tab remains focused and visible; blur, page hide, visibility changes, copy, and cut cancel the timer rather than inspecting or overwriting another clipboard action. The shipped Privacy view explains the boundary, and a canonical local policy route exposes only the content-free policy with role/tenant/audit/review-required controls. Focused service/API/source/mirror/UI tests passed. Browser confirmation, OS clipboard behavior, frozen-runtime, and MSIX proof remain unproven.
134. **Secure temporary-file broker** - Implemented an initial high-risk migration in the source checkout: a central local broker owns bounded, non-repository temporary workspaces, uses owner-only permissions where the platform supports them, rejects symlinked workspace content, emits content-free cleanup receipts, and can recover stale crash remnants. Private document-intelligence Presidio and OCR workspaces now use it instead of system temporary directories. Focused broker and document-intelligence tests passed, with Windows symlink creation skipped where unavailable. Other temporary-file callers remain explicitly un-migrated; frozen-runtime, MSIX, and full-host ACL verification remain unproven.
135. **Parser sandboxing** - Initial document-intelligence worker migration implemented in the source checkout: Docling/Presidio worker launches now require the local Python worker executable/command allow-list, use shell-free invocation, scrub proxy variables, set offline network environment, bound runtime/output, hide Windows worker windows, and apply POSIX CPU/address-space limits where supported. Sandbox denial fails closed into a review-required parser blocker. Focused parser-boundary and document-intelligence tests passed. Archive, image, media, and every remaining parser family are not yet migrated; Windows kernel-level isolation, frozen runtime, and MSIX proof remain unproven.
136. **Content-disarm derivatives** - Implemented a hash-bound, inert UTF-8 plain-text review derivative for supported private records. It requires an explicit per-action approval, leaves immutable originals untouched, strips executable/non-review HTML markup without loading resources, preserves active-matter scope, records a provenance receipt, and exposes both receipt and original-record drill-down in production Document Intelligence. This is not represented as a fidelity-preserving conversion, a redaction, or a safety determination. Focused service/API/production-asset tests passed; frozen-runtime and MSIX proof remain unproven.
137. **Fine-grained capability tokens** - Implemented in the source checkout. The canonical security-session path binds signed opaque capabilities to role, tenant, active matter, allow-listed action, resource type/ID, CSRF, expiry, and a server-side single-use replay ledger. Session minting rejects a non-active matter or arbitrary action, and protected security mutations consume their exact matter resource capability. Production record-source capabilities additionally bind the active matter, record resource, browser-session nonce, role/tenant labels, and an allow-listed read action. All private artifact registries returned by the canonical API—including document intelligence, evidence work products, tracked DOCX copies, session-bound document exports, findings/forms, filing packets, authority impact, and controlled release-evidence artifacts—now retain the same server-side role/tenant/session binding and fail closed for a copied URL. The shipped workbench sends the session header for inspect, document-intelligence, workspace-import, originals, and artifact/export downloads; it fetches opaque artifact URLs rather than navigating directly. Focused source/API/UI tests passed (69 passed; one platform symlink test skipped). Frozen-runtime and MSIX reachability remain unproven.
138. **Local API abuse resistance v2** - Implemented an additional bounded, content-free loopback API burst guard in the production local middleware. Existing exact-host/origin, cross-site, declared/streamed body-size, stale-token, CSRF, and replay checks remain in force; the guard applies separately bounded read/write budgets and returns safe retry metadata without retaining prompts, paths, record IDs, tokens, or bodies. Focused firewall, streaming, security-session, and abuse-guard tests passed. This does not claim a kernel firewall, websocket proof (the current production API exposes no websocket route), frozen-runtime, or MSIX evidence.
139. **Privacy-safe diagnostics** - Implemented an explicit support-bundle flow in the canonical API and shipped Privacy UI. A user can preview exactly four allow-listed non-matter sections and explicitly build a local JSON file; prompts, record text, names, paths, credentials, raw logs, and URLs are excluded by schema rather than merely hidden. The bundle is hash-bound, review-required, and never transferred automatically. Focused service/API/production-asset tests passed; frozen-runtime and MSIX proof remain unproven.
140. **Continuous adversarial corpus** - Implemented a fixed, synthetic-only local adversarial corpus with 14 required cases spanning false premises, fake citations, stale/wrong-jurisdiction authority, prompt/document/OCR-like instruction injection, HTML instruction/resource-load attempts, unsafe archive members, matter-path escape, retrieval SQL-like input, model/tool exfiltration requests, confidentiality leakage, malicious uploads, and filing-ready bypass. The canonical protected API returns only non-sensitive counts/categories, is loopback/RBAC/audit/review-required guarded, and the shipped Privacy UI exposes an explicit local run with a clear boundary: it does not read a matter, call a provider, prove a frozen executable, or replace human release review. Focused source/API/UI and production-browser smoke coverage passed (14/14 synthetic cases); frozen-runtime and MSIX reachability remain unproven.

- **Frozen runtime / MSIX status for slices 131–140 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed ten fictional-data checks in a disposable encrypted matter environment. It verified shipped Privacy UI entry points; clipboard no-read/no-history policy; reviewer/admin, wrong-matter, and unsupported-action fail-closed boundaries; privacy-safe diagnostics preview/build approval; source-bound content disarm with original preservation and hash-verified artifact/receipt drill-down; post-switch artifact denial; and the local synthetic adversarial corpus (14/14 safe). No external TCP connection was observed across 183 samples. The standard corpus fixture deliberately reported a `not_provisioned` key hierarchy and `not_enabled` matter unlock: those truthful, non-secret, review-required states are preserved rather than being promoted to protected/unlocked. This does **not** prove a live Windows Hello prompt, OS clipboard behavior, every parser sandbox, complete temporary-file migration, active-key provisioning for legacy/external corpora, isolated installation, Store, or Enterprise testing. Evidence: `dist/ga_today/evidence/08_v8_privacy_security_e2e_r6_rerun7.json`.

## Wave 15 - Reliability, durability, and observability

141. **Structured local telemetry opt-in** - Implemented in the source checkout. Matter-scoped telemetry defaults to off and refuses to create a metric record until an explicit local-only choice is saved. The only supported opt-in mode permits bounded performance/error counts and allow-listed safe labels; prompts, record text, names, paths, credentials, arbitrary labels, and every remote-export mode are refused. Preferences are atomically written with a content-free hash receipt, an enabled preference records an append-only local preference event, and disabling returns to no-new-metric behavior. Canonical privacy APIs, endpoint inventory, and the shipped Privacy UI expose status, a clear off-by-default choice, and review-required state. Focused source/API/UI tests passed; frozen-runtime and MSIX reachability remain unproven.
142. **Health and dependency dashboard** - Implemented in the source checkout. A user-triggered Local AI controls action now reports content-free API, active-matter durable database, authority, local-model, OCR, media, storage, backup, and UTC clock readiness. It makes missing optional engines and backup configuration visible, never contacts a provider or exposes a path/record/prompt, and stores an encrypted, hash-linked active-matter receipt for each explicit check. The canonical local route returns no result without an active matter; focused service/API/UI-asset tests pass. Frozen-runtime and rebuilt-MSIX reachability remain unproven.
143. **Job journal** - Implemented in the source checkout. The Local AI controls panel now exposes an explicit active-matter runtime journal over the durable kernel: job stage, progress, attempt, cancellation, terminal state, event timestamps, and hashes of inputs/results/errors are visible while all underlying content remains private. Each refresh requires an explicit role, tenant, and browser session, writes an encrypted tenant-bound hash-linked receipt, and refuses absent matter or mismatched tenant state. The canonical local API and production-gateway route inventory are covered by focused synthetic tests; frozen-runtime and rebuilt-MSIX reachability remain unproven.
144. **Idempotency everywhere** - Implemented in the source checkout. The canonical local and enterprise API boundaries now apply one encrypted, bounded duplicate-suppression policy to every JSON POST, PUT, PATCH, and DELETE route. A workbench-generated key binds only to the exact method, route, tenant, role, browser session, active-matter hash, and request hash; an identical retry replays the encrypted local response, while changed payloads, another tenant, a different session, or another matter cannot replay it. File/stream downloads are not buffered: their mutation/session-creation routes are protected, while the existing capability boundary protects the download itself. The AI controls panel explains and inspects the policy. Legacy direct API callers remain supported with an explicit `not_provided` result until a deployment enables required keys. Focused source/API/UI tests passed; frozen-runtime and rebuilt-MSIX reachability remain unproven.
145. **Database integrity monitor** - Implemented in the source checkout. An active-matter, role/tenant/session-protected Local AI controls action opens the durable runtime database read-only, runs bounded SQLite quick and foreign-key checks, detects unreadable/corrupt state, and records an encrypted tenant-bound hash-linked receipt. It returns neither a database path nor table/record/prompt content, removes the raw runtime path from the older kernel-health response, performs no repair/optimization/write operation, and gives conservative preserve-and-recover guidance when blocked. Focused service/API/UI tests passed; frozen-runtime and rebuilt-MSIX reachability remain unproven.
146. **Power-loss resilience tests** - Implemented in the source checkout as an admin-only, active-matter, synthetic fault-injection drill. The shared atomic-write primitive now supports test-only named interruption points after write, after file sync/before replace, and after replace/before directory sync. The disposable drill verifies complete-old-or-complete-new outcomes and no orphan temporary file for synthetic import manifests, review writes, encrypted state, index pointers, backup manifests, and export receipts, then stores an encrypted tenant-bound hash-linked receipt. It never reads a matter record, cuts power, repairs storage, interrupts live work, or certifies physical power-loss/hardware/filesystem/package behavior. Focused service/API/UI tests passed; frozen-runtime and rebuilt-MSIX reachability remain unproven.
147. **Storage-pressure controls** - Implemented in the source checkout. Every durable atomic write and append now preserves a configurable local free-space reserve and fails closed before it can consume the remaining safe capacity. The active-matter Local AI controls forecast reports aggregate capacity, anticipated-write headroom, the enforced write gate, and bounded aggregate orphan-temporary-file cleanup candidates; it never reveals paths or records and never deletes anything. Each check records an encrypted tenant-bound receipt. Focused service/API/UI tests passed; frozen-runtime and rebuilt-MSIX reachability remain unproven.
148. **Clock-skew detection** - Implemented in the source checkout. An active-matter, role/tenant/session-protected local monitor compares UTC wall-clock advance with monotonic-clock advance inside one protected process baseline. A material difference flags audit ordering, deadline candidates, authority freshness, and timestamp certificates for review; it makes no network-time request and never rewrites timestamps. Baselines and receipts are encrypted, tenant-bound, and hash-linked. Focused service/API/UI tests passed; frozen-runtime and rebuilt-MSIX reachability remain unproven.
149. **Performance regression gates** - Implemented an active-matter, role/tenant/session-protected local budget-review path for launch, matter open, mixed import, search, Ask first feedback, draft, packet, peak memory, and MSIX size. The workbench accepts only allow-listed non-negative numeric values, shows each metric's budget and `within_budget`, `over_budget`, or `not_measured` status, and stores an encrypted tenant-bound hash-linked review receipt. Operator-entered and synthetic values stay explicitly unverified, missing values never pass, and the surface refuses prompts, record text, paths, package inventories, machine identifiers, or release certification. Focused service/API/UI/inventory acceptance tests pass; frozen-runtime, installed-package, and CI performance measurements remain unproven.
150. **Failure replay harness** - Implemented an active-matter, role/tenant/session-protected replay of six allow-listed sanitized failure envelopes: local-service unavailability, absent matter, storage reserve protection, pending duplicate action, expired protected dashboard session, and unverified authority. The canonical API accepts only a scenario ID and explicit confirmation, never an exception, request, log, path, prompt, record, identity, or original-operation input. Every replay is explicitly simulation-only, does not re-execute the source operation, exposes its reviewed safe-code/scope/preservation/recovery contract for drill-down, and stores an encrypted tenant-bound hash-linked receipt. Focused service/API/UI/inventory tests pass; no real failure, frozen-runtime, or rebuilt-MSIX replay evidence is claimed.

- **Frozen runtime / MSIX status for slices 141–150 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed nine fictional-data checks. It exercised explicit telemetry opt-in/off, a nine-component content-free dependency dashboard, an empty but bounded job journal, idempotency record/replay of the identical performance-review request, read-only database integrity, storage-pressure and clock-continuity checks, an admin-only synthetic power-loss drill (18 operations), sanitized failure replay, and performance-gate handling that remained incomplete/non-release-eligible. It verified production UI entry markers, active-matter switch behavior, and zero observed external TCP connections across 257 samples. This does **not** prove live workload performance, physical power loss, actual failure recovery, external time correctness, isolated installation, Store, or Enterprise qualification. Evidence: `dist/ga_today/evidence/08_v8_reliability_e2e_r6.json`.

## Wave 16 - Backup, migration, installation, and updates

151. **Incremental encrypted backups** - Implemented a non-destructive v2 upgrade of the existing active-matter Productivity Studio backup path. Each new snapshot encrypts content-addressed chunks separately, reuses only a chunk it decrypts and re-hashes successfully, retains a complete file-to-chunk manifest rather than a parent/delta chain, verifies every manifest and every referenced chunk before marking a backup verified, and restores only into a new recovery directory. Legacy v1 encrypted snapshots remain verifiable/restorable. The shipped backup panel now explains chunk reuse and independent recovery and surfaces format/chunk/reuse fields in its protected review result. Focused synthetic service/API/UI tests pass; no cloud backup, external-media durability, frozen-runtime, or rebuilt-MSIX evidence is claimed.
152. **Point-in-time matter restore** - Implemented a bounded, active-matter snapshot browser that returns only encrypted-snapshot ID, creation time, format, aggregate counts, verification state, restore eligibility, and a manifest-contract drill-down—never a path or record content. A reviewer can select any verified point-in-time snapshot in the shipped Productivity Studio and restore it only as an isolated recovery copy; the original active matter remains unchanged, and the receipt identifies the isolated recovery-matter state without revealing its location. Focused source/API/UI/inventory tests pass; no real-matter, frozen-runtime, external-media, or rebuilt-MSIX recovery proof is claimed.
153. **Cross-device transfer wizard** - Implemented an explicitly user-configured external-transfer boundary for passphrase-encrypted, hash-verified, user-carried bundles. The canonical local API is matter- and role-protected, records encrypted integrity receipts, returns safe opaque metadata only, and the shipped workbench can explicitly export, inspect, and import a bundle. Import verifies every manifest and file hash before creating an isolated recovery copy; it never merges or overwrites the active matter, discovers a device, or makes a network request. Focused source/API/production-asset tests pass; no actual second device, frozen-runtime, rebuilt-MSIX, or external-media recovery proof is claimed.
154. **Schema migration laboratory** - Implemented an encrypted, matter-scoped synthetic profile-contract laboratory for the declared 6.0.3.0, 6.0.4.0, 7.0.0.0, and 8.0.0.0 source contracts. It exercises clean upgrade, interruption before commit, and interruption after commit, verifies preservation-required profile fields separately from the expected schema change, records hash-linked encrypted receipts, enforces tenant scope, and exposes only safe aggregate evidence in the shipped Local Workbench. It does not open, change, or copy a live matter; it does not qualify a frozen installer, MSIX upgrade, or rollback package. Focused source/API/production-asset tests pass.
155. **MSIX upgrade qualification automation** - Implemented a fail-closed planner that parses each exact MSIX manifest, validates immutable Store identity, publisher, x64 architecture, executable, en-us language, and monotonic version, then hash-binds the prior and candidate packages to a required isolated-runner sequence: prior install, fictional state creation, upgrade, launch/API/UI verification, restart, uninstall/reinstall, and retention-choice verification. It refuses a substituted identity or incomplete runner evidence and never modifies the user’s Store package. The actual v7.0.1→v8.0.0 contract is compatible and ready for an isolated runner; no isolated installation, upgrade, uninstall, or reinstall was available or claimed.
156. **WACK automation and report parser** - Implemented exact-package WACK discovery and a hash-bound parser that preserves the report, classifies failures and warnings, rejects missing/unparseable reports, and blocks Store release unless completed execution has zero classified failures. The runner now defaults to v8.0.0 rather than a stale v7 package and never treats an appcert process launch as success without exit/report validation. The kit is present on this machine but elevation is unavailable, so current v8 evidence is correctly `not_run` and Store-blocked rather than certified. Focused parser tests pass.
157. **Store asset validator** - Implemented a fail-closed v8 Store validator for true PNG encoding, exact hashes, desktop dimensions, 50-MB limit, landscape orientation, caption/manifest agreement, caption length, screenshot-archive completeness, privacy-link presence, private marker scanning, unsupported release claims, and the frozen accepted-feature scope. It caught two undersized legacy screenshots; faithful deterministic 1366×768 siblings were created from the same fictional captures and the submission ZIP/manifest/captions were refreshed. All six current screenshots validate, as do listing privacy and scope boundaries. This does not override WACK, package-install, or Store certification blockers.
158. **Package-size optimization** - Implemented exact archive-level MSIX size accounting, top-entry reporting, tier budgets, and an offline build-tier contract. The v8 full package passes its full-tier budget and identifies approximately 1.65 GB of optional full document-intelligence payloads (spaCy vectors, Torch, Docling, OpenCV, and RapidOCR). The canonical build now validates the selected tier after package creation. An essential tier is a separately built/signed offline package that must exclude the full-model prefixes; neither tier may download, side-load, or activate payloads at runtime. Focused validator tests and the existing v8 full archive pass. A new essential-tier MSIX still requires its own build and qualification before it may be offered.
159. **Rollback preparation** - Implemented a fail-closed, hash-bound rollback-rehearsal planner for the exact candidate and compatible fallback MSIX packages. It requires independent encrypted-backup recovery evidence for a fictional isolated matter, fixes the seven required operator steps, rejects identity/version/backup mismatches, and explicitly forbids automatic user-device rollback or active-matter modification. A runner-result validator accepts only a full isolated rehearsal with fictional-data and preservation confirmations. Focused planner tests pass. Current v8 rollback evidence remains blocked until compatible v8 backup recovery and an actual isolated rehearsal are supplied; no rollback is claimed or performed.
160. **Signed update metadata** - Implemented an Ed25519 verifier for a release-ceremony-signed metadata record bound to one exact MSIX identity, hash, and frozen release-scope hash. It refuses an untrusted/tampered signature, substituted package, scope mismatch, and any URL, installer command, auto-install, provider endpoint, or delivery authority other than Windows Store/Partner Center. The shipped trust configuration intentionally has no production key and the current v8 evidence is blocked until an authorized ceremony supplies signed metadata and a trusted public key. Focused verifier tests pass; the verifier makes no network request, download, install, publish, or update-control claim.

- **Frozen runtime / MSIX status for slices 151–154 (current r6 candidate):** the exact `8.0.0.0` package hash `9d409b946c9d3fd26fd824d39fda69358ec789b8d38c824e146b00889b3ac46a` passed six zero-network checks using only a disposable fictional matter and an explicitly configured disposable sibling transfer destination. It scheduled and created an `incremental_encrypted_chunks_v2` backup, verified it, restored only to an independent recovery copy, created/listed/imported an encrypted user-carried bundle without merging or overwriting the active matter, and ran the review-required `synthetic_migration_contract` for 6.0.4.0 interruption-after-commit without changing the live matter. The exact frozen production HTML and workbench asset exposed the corresponding backup, transfer, and migration UI entries; 8 focused source/service/API/UI tests also passed. This is not second-device or removable-media durability, live package upgrade, WACK, signing, Store, Enterprise, or real-matter evidence. Evidence: `dist/ga_today/evidence/08_v8_backup_transfer_e2e_r6_rerun1.json` and `08_v8_backup_transfer_focused_tests_rerun1.xml`.

## Wave 17 - Enterprise administration and governance

161. **Admin console acceptance** - Implemented a production-gateway administration review endpoint and shipped Local AI controls panel. It requires an admin role and tenant scope, returns only the caller’s tenant boundary, supported-role policy, policy hashes, authority status, release-evidence hashes, and explicit blocked-export reasons, and stores an encrypted tenant-bound receipt for an explicit refresh. It deliberately does not enumerate or provision people: identity lifecycle remains an external enterprise control. Focused production-route, role/tenant, encrypted-receipt, source/mirror UI, and syntax tests pass. Frozen-runtime, rebuilt-MSIX, external identity-provider, and live enterprise deployment evidence remain unproven.
162. **Role-policy simulator** - Implemented an admin-only, tenant-scoped fictional role preview in the shipped Local AI controls panel and production gateway. The simulator accepts only known roles and allow-listed permissions, returns each allow/deny decision with the granting-role policy basis or a precise denial reason, writes an encrypted tenant-bound receipt, and refuses unknown roles. It cannot create a user, change a role, alter a policy pack, authorize an export, or use a network. Focused service/API/UI/inventory tests pass; external identity, frozen-runtime, rebuilt-MSIX, and real policy-change evidence remain unproven.
163. **Separation-of-duties rules** - Implemented a shipped, admin-only, tenant-scoped review gate for four opaque approval references: authority activation, security approval, legal sign-off, and release approval. It requires the exact role at each stage, refuses malformed input, and fails closed whenever an actor reference is reused, a role mismatches, or a required approval is absent. Explicit reviews receive encrypted tenant-bound, hash-chained receipts and source drill-down to the supplied opaque artifact reference. It cannot activate authority, create a legal sign-off, approve a release, expose people, or use a network. Focused service/API/UI/mirror/inventory checks pass; real identity-provider, accountable human, external sign-off, frozen-runtime, and rebuilt-MSIX evidence remain unproven.
164. **Policy-pack lifecycle** - Implemented a hardened v2 lifecycle in the shipped Local AI controls panel and production gateway: encrypted tenant-scoped draft, fixed-baseline validation, hash-bound diff, external Ed25519 signature verification, activation only when both validation and a trusted signature pass, expiration, and rollback that reactivates the prior eligible signed pack. A blank repository trust store means a real activation remains blocked until an external governance ceremony provides a trusted public key and signature; no key, identity, network call, or fake approval is created locally. Focused lifecycle, failure, role/tenant, source/mirror UI, inventory, and syntax checks pass. Real accountable approvals, policy-enforcement rollout, frozen-runtime, and rebuilt-MSIX evidence remain unproven.
165. **Legal-hold controls** - Implemented an encrypted, tenant-bound, active-matter hold register with opaque hold, artifact, and authority references; hash-chained audit; review-required list and release receipts; and an admin-only production UI/API path. Crucially, the actual production document-workspace delete-request and delete-commit paths now check active holds and fail closed, preserving a held artifact until a separate explicit release authority reference is recorded. Focused fictional end-to-end route coverage proves place → delete blocked → release → existing two-phase delete works; legacy document-workspace tests also pass. A local hold record does not decide a real legal obligation, and real authority, frozen-runtime, and rebuilt-MSIX evidence remain unproven.
166. **Retention-policy engine** - Implemented encrypted tenant-scoped, active-matter retention-plan preview and application with artifact-level hold checks, configured policy drill-down, hash-chained receipts, approval-reference verification, recovery-window state, and cancellation. The shipped approval configuration is intentionally empty, so production application fails closed until an organization supplies an approved opaque policy reference; tests prove the eligible path using a synthetic external config. Applying starts only a recoverable review window and never deletes, exports, or transmits a family record. Focused production API/UI and direct hold/approval/recovery tests pass; real organizational policy, automated disposition, frozen-runtime, and rebuilt-MSIX evidence remain unproven.
167. **Audit verification console** - Implemented a production admin console that decrypts and verifies tenant-scoped legal-hold, retention-plan, and signed-policy-pack audit metadata; checks chained event hashes, prior-hash gaps, invalid or regressing clocks, and recorded policy-pack signature state; and issues an encrypted local receipt for a privacy-safe scoped report. Synthetic hash tampering is detected and blocks the report. The UI/API never exposes private matter text, raw paths, identity directories, or a fabricated external audit opinion, and it makes no network request. Focused production-route/UI, encrypted-receipt, and tamper detection tests pass; independent audit, frozen-runtime, and rebuilt-MSIX evidence remain unproven.
168. **Enterprise configuration export** - Implemented a production admin manifest builder that returns only selected configuration and evidence hashes plus explicit privacy/delivery boundaries, excluding matter content, databases, credentials, raw paths, and logs. The exact in-memory manifest can be verified against an externally provisioned Ed25519 public key and creates an encrypted tenant-bound receipt; untrusted signatures fail closed, and the default repository trust store deliberately has no production key. Focused external-signature, privacy, API, and shipped source/mirror UI tests pass. No actual organization signature, network export, frozen-runtime, or rebuilt-MSIX evidence is claimed.
169. **Offline license/entitlement model** - Implemented an optional, entirely offline Ed25519 entitlement verifier with encrypted tenant-bound state and an admin UI/API. The shipped FOSS trust config keeps commercial mode disabled, so no entitlement is required. If separately commercialized, a deployment may provision a trusted public key and verify a signed opaque entitlement locally; the contract forbids telemetry, provider requests, matter content, cross-tenant access, and any matter-access decision. Focused FOSS, signed test-entitlement, API, and source/mirror UI tests pass. No production key, billing, commercial entitlement, frozen-runtime, or MSIX evidence is claimed.
170. **Organization readiness dashboard** - Implemented an admin-only production dashboard with independent engineering, legal, security, privacy, operations, accessibility, pilot, and Microsoft Store decision lanes, per-lane blockers, local governance evidence hashes, and encrypted tenant-bound refresh receipts. It explicitly reports `not_ready_for_enterprise_ga`: engineering foundations do not fabricate attorney review, independent security assessment, accessibility proof, pilot evidence, package qualification, Store submission evidence, or owner approval. Focused service/API/UI/mirror/inventory checks pass; all external approval and frozen/package evidence remains unproven.

## Wave 18 - Collaboration and interoperability

171. **Reviewer bundle round trip** - Implemented an encrypted active-matter reviewer-bundle ledger through the canonical production gateway and shipped source/mirror workbench entry. A user can create a local-only bundle from a handoff manifest, attach a record-bound encrypted comment, record an explicitly non-cryptographic reviewer attestation, reimport the exact hash-verified bundle, inspect exact-record locators, and reconcile append-only lineage. Viewer-role mutations are denied; tampered, cross-handoff, changed-manifest, and duplicate reimports fail closed. Reconciliation never merges, transmits, signs legally, or changes filing status. Focused service/production-gateway/source-mirror tests pass; browser, frozen-runtime, package, external signature, and human-review evidence are not claimed.
172. **Structured comment threads** - Implemented encrypted, active-matter comment threads for hash-bound private-record spans, official-source spans, claims, draft revisions, and artifacts. Threads support append-only replies and a review-only resolution state; they never edit the target, merge work, transmit messages, or decide facts, law, or filing status. Inventory deliberately excludes comment bodies. Viewer mutation is denied, target locators stay visible, and focused service/production-gateway/source-mirror acceptance passes. Browser, frozen-runtime, package, external messaging, and human-review evidence are not claimed.
173. **Review assignment queue** - Implemented encrypted active-matter review assignments with an explicit role, safe assignee reference, ISO due date, hash-bound scope, required evidence, append-only claim, and completion only after the assigned reviewer acknowledges every required evidence reference. Completion remains `completed_review_required`, not approval or filing readiness. The queue sends no message, creates no account, and performs no external action. Focused service/production-gateway/source-mirror acceptance passes; browser, frozen-runtime, package, external messaging, and human-review evidence are not claimed.
174. **Conflict-aware merge** - Implemented an encrypted active-matter merge planner for two independent review-bundle manifests. It detects divergent item hashes, blocks finalization while any conflict remains unresolved, records explicit left/right/defer choices, and only then emits a new local review-required merged bundle. It never applies the output to a matter, overwrites either source bundle, sends data, or treats a choice as legal/factual approval. Focused service/production-gateway/source-mirror acceptance passes; browser, frozen-runtime, package, external signature, and human-review evidence are not claimed.
175. **Standards-based calendar export v2** - Implemented an explicit local ICS preview from encrypted source-bound calendar events. It supplies stable event IDs/UIDs, IANA timezone, sequence/update number, recurrence validation, display alarms, and cancellation status while recording an encrypted receipt. The canonical production route requires a reviewer-capable role, denies viewer export, is covered by the production gateway inventory, and has a shipped source/mirror control. The export never downloads automatically or writes to any calendar account. Focused service/production-gateway/source-mirror tests pass; browser, frozen-runtime, package, and third-party-calendar interoperability evidence are not claimed.
176. **Email export package** - Implemented a reviewer-gated, encrypted active-matter EML/ZIP handoff builder from existing email-integrity exports. It requires explicit privacy acknowledgement, records a hash-bound manifest and encrypted receipt, preserves source locators, emits local EML and ZIP bytes only, and never sends mail, chooses a recipient, downloads a file, or asserts delivery/authenticity. Viewer mutations are denied and a source/mirror production workbench control exposes the same boundary and exact-record drill-down. Focused service/production-gateway/source-mirror tests pass; browser, frozen-runtime, package, external delivery, and human-review evidence are not claimed.
177. **PDF/A review export** - Implemented a source-bound local PDF review-derivative path with an encrypted active-matter receipt ledger, exact source hashes and locators, reviewer role boundary, visible review-required state, and a shipped source/mirror control. It generates a readable PDF artifact without automatically downloading it. The implementation intentionally reports `pdf_a_conformance: not_verified` because no configured converter and independent conformance validator are bundled; that limitation is visible in the generated PDF, API receipt, and UI. It never asserts a filing artifact, preservation certification, or PDF/A compliance. Focused service/production-gateway/source-mirror tests pass; browser, frozen-runtime, packaged-app, and external archival-validation evidence are not claimed.
178. **CSV/JSON evidence export** - Implemented a reviewer-gated, encrypted active-matter receipt ledger that creates an explicit scoped JSON and/or CSV evidence package. JSON exposes its schema, scope, hash-bound source locator, review state, and review-required boundary at the top level; CSV carries the same row-level fields. The UI exposes a meaningful one-row fictional action, source drill-down, privacy acknowledgement, and receipt inspection. It never exports the matter database, writes to an external destination, or downloads automatically. Focused service/production-gateway/source-mirror tests pass; browser, frozen-runtime, packaged-app, multi-row production-scale, and external-transfer evidence are not claimed.
179. **Print workflow** - Implemented a reviewer-gated, encrypted active-matter print-preview ledger with a shipped high-contrast print view. A user can prepare a source-bound review preview with a confidentiality marking, page header, exact source boundary, and review footer, then record a separate request before opening a browser print dialog. The app never silently prints or selects a printer; the dialog exposes its own explicit “Choose printer and print” control. Focused service/production-gateway/source-mirror tests pass; browser/device-printer, frozen-runtime, and packaged-app evidence are not claimed.
180. **External-tool boundary receipts** - Implemented encrypted active-matter receipts for a declared external-tool boundary: export ID/hash, purpose, opaque actor, destination class, exact source scope, privacy-risk acknowledgement, and explicit review state. A receipt does not transmit data, retain a destination address, or verify delivery. If a user reports an external transfer, the receipt says `self_reported_transfer_unverified`; otherwise it says `planned_not_performed`. Focused service/production-gateway/source-mirror tests pass; browser, frozen-runtime, packaged-app, external transfer, and human-review evidence are not claimed.

## Wave 19 - Evaluation, legal quality, and pilot readiness

181. **Attorney gold-data workbench** - Implemented the safe preparation path on top of the existing canonical human-evaluation ledger: a shipped release-control UI can create blinded hash-only synthetic/public-authority tasks and inspect immutable readiness blockers through the production gateway. The ledger already supports independent review and adjudication artifacts, but the UI deliberately does not claim to verify an attorney’s license or submit a reviewer attestation merely from a local role header. A prepared task remains `awaiting_independent_reviews`; synthetic rows remain non-attorney evidence until qualified external reviewers and credential/licensing evidence are supplied. Focused production-route/source-mirror/inventory tests pass. Actual attorney review, adjudication, licensing verification, reviewed evaluation data, frozen-runtime, and packaged-app evidence remain blockers and are not claimed.
182. **Retrieval benchmark expansion** - Hardened the canonical external attorney-gold retrieval evaluator and shipped workbench display. Production evaluation now requires, per counted row, issue labels, an authority-build ID, source snapshot hash, reviewer-evidence hash, externally verified license status, and current/fresh source status. It reports issue/freshness coverage, provenance-complete rows, Recall@5/10/20, MRR, nDCG@20, exact-citation accuracy, and an explicit `pinpoint_accuracy: not measured` limitation because the current source-ID contract cannot prove pinpoints. The production route now applies the reviewer source-access boundary. Focused strict/legacy evaluator and source/mirror UI tests pass. No configured real authority index or qualified external attorney-gold dataset was available, so live metrics, attorney review, pinpoint verification, frozen-runtime, and packaged-app evidence remain unproven.
183. **Claim-verifier benchmark** - Implemented a strict external-evidence seven-state claim-verifier benchmark and a reviewer/tenant-scoped shipped release-control entry. The production run requires per-counted-row issue labels, authority-build ID, source-snapshot hash, reviewer-evidence hash, externally verified license status, and current/fresh source status; it reports per-state accuracy, actual-state counts, issue/freshness coverage, and maps the gold label `unknown` only to the verifier's explicit `not_verifiable` result. Missing provenance or any of the seven outcome labels blocks the metric, and the UI redacts external paths and evaluation text. Focused strict/legacy runner, role/tenant, production-route, source/mirror UI, syntax, and inventory checks pass. No real external attorney-gold dataset, licensed-review evidence, live authority build, browser/frozen-runtime, or packaged-app evidence is claimed.
184. **Quote-verifier benchmark** - Implemented a strict external-evidence parser-variant quote benchmark and a reviewer/tenant-scoped shipped release-control entry. The strict run requires current/fresh, licensed, hash-provenanced rows and reports exact, normalized, fuzzy-review-required, mismatch, and not-found decision coverage by parser variant. Normalized whitespace/punctuation matches now retain original-source offsets; fuzzy/semantic candidates remain explicit review-required candidates and never become an exact quote. Missing provenance, a missing parser variant, or a missing required decision blocks the metric; UI results redact external paths and quoted row content. Focused strict/legacy runner, quote-offset, role/tenant, production-route, source/mirror UI, syntax, and inventory checks pass. No real attorney-reviewed quote dataset, live authority build, browser/frozen-runtime, or packaged-app evidence is claimed.
185. **Procedural safety evaluation** - Implemented an external reviewed-scenario harness through the canonical filing gate, reviewer/tenant production API, and shipped release-control UI. Strict runs require six covered categories (deadline, service, posture, forms, venue, filing), an authority-build ID, source/reviewer hashes, verified license state, current/fresh source status, qualified attorney-review metadata, opaque gate payload, and expected blocker codes. Every expected blocker must remain blocked by the filing gate; missing provenance, category coverage, or a false pass blocks the metric. The UI exposes only counts/codes and redacts external scenario payloads. Focused gate, strict evaluator, role/tenant, source/mirror UI, syntax, and inventory checks pass. The passing fixture proves the software contract only; reviewed real scenario data, browser/frozen-runtime, and packaged-app evidence remain unproven.
186. **Bias and accessibility evaluation** - Implemented a strict external-case automated safeguard evaluator plus reviewer/tenant release-control UI/API. It covers language, disability, literacy, cultural, and self-represented-user scenarios; each counted row requires authority/reviewer hashes, verified license state, current/fresh source, qualified attorney-review metadata, and one category. It verifies review-required, source/freshness visibility, plain-language behavior where required, and absence of unsafe certainty; missing provenance/category coverage fails closed. Existing keyboard, focus, contrast-token, reduced-motion, screen-reader-mode, and source/mirror checks also pass after repairing source/mirror JS drift. This is explicitly **not** a substitute for user testing by affected people, assistive-technology testing, independent accessibility assessment, or bias review; all remain external evidence blockers.
187. **Longitudinal matter evaluation** - Implemented a tenant-scoped production release-control route and shipped UI entry for a disposable, fictional longitudinal software-contract evaluation. It composes encrypted intake history, source-bound timeline correction and drill-down, command-center snapshot stale detection after a changed authority fixture, reopen/restart persistence, and the synthetic schema migration/recovery lab. The strict external scenario manifest requires every named scenario, synthetic data classification, fixture identity, two content hashes, and an explicit non-attorney-evidence marker; missing coverage or metadata blocks the run. The route returns only statuses, counters, and codes—not fixture content, filesystem paths, or any active-matter data. Focused runner, encryption/history/staleness/migration behavior, role/tenant, inventory, source/mirror UI, and syntax checks are recorded. This is not attorney review, a live-matter migration, pilot evidence, frozen-runtime proof, packaged-app proof, or release readiness.
188. **Controlled attorney sandbox** - The source checkout now contains the operational, external-root attorney-sandbox program/cohort/assignment/structured-review/feedback-triage/evidence-packet workflow. The production dispatcher was hardened so the protected enterprise route family is authoritative over duplicate legacy local routes, preserving role/tenant middleware, local-request firewall, idempotency, and audit headers. The software may prepare and audit the no-private-data workflow, but it cannot verify a reviewer identity or license, conduct an attorney review, or mark Pass 48 complete; the status remains `ready_for_external_pass48_gate` at most and requires external signed evidence.
189. **Controlled pilot operations** - The source checkout contains the opaque-ID/hash-only limited-real-matter pilot controls for enrollment, consent evidence references, tenant allowlists, encrypted-store evidence references, daily review, export restrictions, incidents, stop conditions, signoff, and tamper-evident evidence packets. The same production gateway hardening makes the protected enterprise controls authoritative. Synthetic software tests validate refusal of training use, unauthorized tenants, unsupported filing-ready exports, and leakage incidents; they do not constitute real-matter consent, attorney participation, pilot results, or Pass 49 completion. External evidence remains required.
190. **Release metric eligibility gate** - Implemented a fail-closed external release-metric evidence gate with a canonical reviewer/tenant production route and shipped release-control entry. Every thresholded metric must first pass the existing sample/threshold/attorney-review audit, then supply a non-synthetic `external_attorney_reviewed` classification, externally verified license status, authority-build ID, source/reviewer/license/reproducibility hashes, two independent reproduction runs, and membership in a hash-only external artifact manifest. The full measurement bundle and artifact manifest must be covered by an Ed25519 attestation whose key is in an externally provisioned trust configuration; filesystem paths, reviewer identities, source text, and matter data are not exposed through the UI. A passing contract is only eligible for external enterprise review—not a substitute for qualified attorney review, pilot evidence, organizational sign-offs, or an Enterprise GA decision. Focused production-route/source-mirror/inventory acceptance passes; configured real external evidence, governance validation of reviewer credentials, browser/frozen-runtime, and packaged-app evidence remain unproven.

## Wave 20 - Platform scale and Enterprise GA closure

191. **Jurisdiction pack interface** - Implemented a signed external jurisdiction-pack catalog with a canonical reviewer/tenant production API, encrypted tenant-and-matter selection receipt, source-hash drill-down, and shipped release-control UI. A pack must supply separate hashes for authority, citation, forms, procedure, terminology, safety policy, and index snapshot, plus controls that prohibit non-local operation and jurisdiction determinations. Its Ed25519 signature must verify against an external trust configuration before selection. The selected pack state is encrypted and tenant/matter scoped with chained audit events; cross-tenant selection lookup returns no selection. Packs contain metadata only: no authority is packaged, a selection is review-required, and it cannot decide jurisdiction or make a filing-ready claim. Focused service/production-route/source-mirror/inventory acceptance passes; real multi-jurisdiction authority, browser/frozen-runtime, packaged-app, and legal review evidence are not claimed.
192. **Second-state reference implementation** - Implemented a signed `XX-AURORA` fictional reference pack against the same catalog and production UI/API boundary. It carries the full component-hash/control contract, validates under an external trust key, and appears as `reference_verified_not_selectable`; activation is expressly refused. The reference builder requires a caller-supplied key and never creates or persists a signing credential or legal content. This proves the configuration interface without representing a second real state, a jurisdiction determination, legal expansion, attorney review, frozen-runtime, or packaged-app evidence.
193. **Extension SDK sandbox** - Hardened the existing encrypted, matter-scoped Add-on Studio extension path into a declarative sandbox. A legacy self-supplied key can still register a disabled artifact for compatibility, but it cannot enable or run it. Enablement and execution require an Ed25519 signature from an externally configured trust key over a versioned sandbox contract declaring allowed operations, a per-matter quota, and a memory ceiling. The only current operation is a deterministic source-metadata digest: it receives a safe source ID/hash, produces a review-required hash receipt, and has no network, arbitrary tool, or arbitrary code-execution access. Quota exhaustion, missing permission, untrusted key, inactive extension, and revocation fail closed. The shipped Add-on Studio now exposes the sandbox run action; focused service/production-route/source-mirror acceptance passes. This is a deliberately declarative capability boundary, not general third-party code execution, extension certification, browser/frozen-runtime, or packaged-app evidence.
194. **Extension review and certification kit** - Implemented a canonical Add-on Studio certification assessment and shipped reviewer UI entry for externally trusted declarative extensions. The assessment records static declarative/no-network/no-tool constraints, a dependency audit, adversarial control expectations (untrusted key, undeclared operation, quota, and revocation), and a user-boundary review requiring review-required and no-network language. It emits a hash-bound `certification_review_required` record only. A separate Ed25519-signed external admission must bind the exact manifest and certificate hashes before an `admitted_review_required` receipt exists; altered decisions or hashes fail closed. This is not a general code scanner, independent security review, third-party certification, browser/frozen-runtime, or packaged-app evidence.
195. **Public API stability program** - Implemented a versioned declared-contract snapshot, externally stored frozen baseline, compatibility comparison, deprecation-warning/migration-action reporting, canonical reviewer/tenant production routes, and shipped release-control UI. The comparator treats removed or changed endpoint contracts as blockers and flags added endpoints for review; it never publishes, changes, or contacts an API. The baseline must remain outside the source tree and its path is redacted from UI/API results. Focused comparator/production-route/source-mirror/inventory tests pass. No external consumer adoption, published API SLA, browser/frozen-runtime, or packaged-app compatibility evidence is claimed.
196. **Exact package SBOM and vulnerability boundary** - Implemented a canonical reviewer/tenant production route and shipped release-control entry that inventories one configured MSIX archive with per-member hashes/origins, a stable payload-manifest hash, parsed package identity, exact-source SBOM hash/component count, and bundled license/notice presence. It rejects unsafe/duplicate archive members and requires a separate external vulnerability bundle bound to the exact package and source hashes with passing Syft, Grype, pip-audit, and Semgrep records and zero blocking findings. The UI/API return only safe counts, hashes, codes, and identity—not package/evidence paths or contents. Focused fixture, tamper, route/inventory, source/mirror, and privacy-redaction checks pass. No real candidate MSIX, external vulnerability scan, signing, install, WACK, Store submission, frozen-runtime, or package qualification is claimed.
197. **Reproducible release evidence** - Implemented a fail-closed, signed external reproducibility verifier with a canonical reviewer/tenant production route and shipped release-control action. It requires at least two independent passing build records matching the exact current MSIX hash, payload-manifest hash, source-SBOM hash, and one pinned toolchain-manifest hash, plus an Ed25519 attestation from an externally provisioned trust configuration. Any altered run or attestation fails closed. It does not build, sign, upload, publish, or certify a package. Focused independent-run, tamper, route/inventory, source/mirror, and external-path-redaction checks pass; reproducible builds of a real signed Store candidate remain external evidence.
198. **Incident response program** - Implemented a content-free production release-control path with defined P0–P3 severity, containment, hash-only evidence preservation, user-notice preparation, recovery, and postmortem templates. A reviewer can run one allow-listed fictional tabletop scenario and receives an immutable plan hash, action count, review-required state, and an explicit external owner-training/tabletop-evidence blocker. It never opens a matter, contacts a user, modifies an account, creates a live incident, or treats a local fixture as operational incident readiness. Focused fictional tabletop, route/inventory, source/mirror, and privacy checks pass.
199. **Required organizational sign-offs** - Implemented a canonical reviewer/tenant verifier and workbench entry for an externally stored, Ed25519-attested legal/security/privacy/accessibility/product/operations/release approval bundle. Every lane needs a hash-bound authorization-evidence reference; missing lanes, malformed bundles, untrusted keys, altered hashes, and bad signatures fail closed. Even a syntactically complete signed bundle remains `blocked` until external governance validates that its signers had real organizational authority. The application cannot capture or invent an actual legal, security, privacy, accessibility, product, operations, or release approval. Focused coverage proves the evidence contract only.
200. **Enterprise GA decision packet** - Implemented a canonical, review-required two-axis decision-packet assembler and shipped workbench entry. It accepts only a signed external manifest that hash-binds live authority, attorney evaluation, security assessment, controlled pilot, Store qualification, rollback, support, organizational signoffs, package provenance, reproducibility, and incident response evidence. Missing or altered categories fail closed; even a complete fixture returns `STORE_GA_BLOCKED` and `ENTERPRISE_GA_BLOCKED` because this code has no authority to certify a Store submission or organizational Enterprise GA decision. Focused route/inventory, source/mirror, tamper, and no-external-path checks pass. Actual human evidence, qualified MSIX/install/offline/WACK/Store evidence, and authorized release decisions remain external blockers.

## Recommended execution gates

- **Gate A - Best immediate product return:** slices 1-20.
- **Gate B - Trustworthy legal research:** slices 21-40.
- **Gate C - Complete core work product:** slices 41-100.
- **Gate D - Fast and dependable local platform:** slices 101-160.
- **Gate E - Enterprise operations and external validation:** slices 161-200.

Do not version-bump merely because a wave is coded. A wave is release-eligible only after its accepted-feature manifest, production UI evidence, frozen-runtime evidence, package-boundary audit, and honest blocker report are complete.
